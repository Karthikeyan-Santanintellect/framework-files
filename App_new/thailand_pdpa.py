"""Loader — Thailand PDPA.

Source folder : New sources/Thailand PDPA
Source document(s):
    KB 2/Thailand/Personal Data Protection Act.pdf

Graph shape   : 8 node labels, 9 relationship types
Expected size : 569 nodes / 691 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/thailand_pdpa.py --dry-run   # validate, write nothing
    python App_new/thailand_pdpa.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'THAILAND_PDPA'
FOLDER = 'Thailand PDPA'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'THAILAND_PDPA'})
SET f.name = 'Thailand PDPA',
    f.jurisdiction = 'Thailand',
    f.folder = 'Thailand PDPA',
    f.source_document = 'KB 2/Thailand/Personal Data Protection Act.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'THAILAND_PDPA'})
MATCH (n:NewSourceNode {framework_id: 'THAILAND_PDPA'})
WHERE NOT (:NewSourceNode {framework_id: 'THAILAND_PDPA'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Chapter.csv  (9 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.chapter_label = row.chapter_label,
      n.chapter_title = row.chapter_title,
      n.first_section = row.first_section,
      n.last_section = row.last_section,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (414 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.section_id = row.section_id,
      n.clause_label = row.clause_label,
      n.clause_type = row.clause_type,
      n.parent_clause_id = row.parent_clause_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DataSubjectRight.csv  (8 rows, key right_id)
node_datasubjectright = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.right_id})
  SET
      n:DataSubjectRight,
      n.right_id = row.right_id,
      n.right_name = row.right_name,
      n.section_id = row.section_id,
      n.clause_id = row.clause_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (10 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.defined_in_section = row.defined_in_section,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_LawfulBasis.csv  (13 rows, key lawfulbasis_id)
node_lawfulbasis = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.lawfulbasis_id})
  SET
      n:LawfulBasis,
      n.lawfulbasis_id = row.lawfulbasis_id,
      n.basis_name = row.basis_name,
      n.section_id = row.section_id,
      n.clause_id = row.clause_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (5 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.chapter_id = row.chapter_id,
      n.part_label = row.part_label,
      n.part_title = row.part_title,
      n.first_section = row.first_section,
      n.last_section = row.last_section,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (14 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.section_id = row.section_id,
      n.liability_type = row.liability_type,
      n.addressee = row.addressee,
      n.sanction_text = row.sanction_text,
      n.max_imprisonment = row.max_imprisonment,
      n.max_fine = row.max_fine,
      n.note = row.note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (96 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.section_number = row.section_number,
      n.chapter_id = row.chapter_id,
      n.part_id = row.part_id,
      n.clause_count = row.clause_count,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CONTAINS.csv -> :HAS_PART  (5 rows)
rel_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_PART]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_CONTAINS.csv -> :HAS_SECTION  (96 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (10 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_GRANTS_RIGHT.csv -> :GRANTS_RIGHT  (8 rows)
rel_grants_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'GRANTS_RIGHT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:GRANTS_RIGHT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (226 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_SUBCLAUSE  (188 rows)
rel_has_subclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBCLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBCLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_PENALTY.csv -> :IMPOSES_PENALTY  (14 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_PROVIDES_BASIS.csv -> :PROVIDES_BASIS  (13 rows)
rel_provides_basis = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PROVIDES_BASIS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:PROVIDES_BASIS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES.csv -> :REFERENCES  (131 rows)
rel_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'THAILAND_PDPA', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES]->(t)
  SET r.citation = row.citation
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Thailand PDPA',
    folder=FOLDER,
    jurisdiction='Thailand',
    source_document='KB 2/Thailand/Personal Data Protection Act.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['chapter_label', 'chapter_title', 'first_section', 'last_section', 'source_document'],
                 rows=9, loadable=9, cypher=node_chapter),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['section_id', 'clause_label', 'clause_type', 'parent_clause_id', 'text', 'source_document'],
                 rows=414, loadable=414, cypher=node_clause),
        NodeStep(label='DataSubjectRight', csv_name='nodes_DataSubjectRight.csv', id_column='right_id',
                 properties=['right_name', 'section_id', 'clause_id', 'source_document'],
                 rows=8, loadable=8, cypher=node_datasubjectright),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition_text', 'defined_in_section', 'source_document'],
                 rows=10, loadable=10, cypher=node_definition),
        NodeStep(label='LawfulBasis', csv_name='nodes_LawfulBasis.csv', id_column='lawfulbasis_id',
                 properties=['basis_name', 'section_id', 'clause_id', 'source_document'],
                 rows=13, loadable=13, cypher=node_lawfulbasis),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['chapter_id', 'part_label', 'part_title', 'first_section', 'last_section', 'source_document'],
                 rows=5, loadable=5, cypher=node_part),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['section_id', 'liability_type', 'addressee', 'sanction_text', 'max_imprisonment', 'max_fine', 'note', 'source_document'],
                 rows=14, loadable=14, cypher=node_penalty),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['section_number', 'chapter_id', 'part_id', 'clause_count', 'source_document'],
                 rows=96, loadable=96, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='HAS_PART', csv_name='rels_CONTAINS.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_has_part),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_CONTAINS.csv',
                properties=[],
                rows=96, loadable=96, cypher=rel_has_section),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=10, loadable=10, cypher=rel_defines),
        RelStep(rel_type='GRANTS_RIGHT', csv_name='rels_GRANTS_RIGHT.csv',
                properties=[],
                rows=8, loadable=8, cypher=rel_grants_right),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=[],
                rows=226, loadable=226, cypher=rel_has_clause),
        RelStep(rel_type='HAS_SUBCLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=[],
                rows=188, loadable=188, cypher=rel_has_subclause),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_IMPOSES_PENALTY.csv',
                properties=[],
                rows=14, loadable=14, cypher=rel_imposes_penalty),
        RelStep(rel_type='PROVIDES_BASIS', csv_name='rels_PROVIDES_BASIS.csv',
                properties=[],
                rows=13, loadable=13, cypher=rel_provides_basis),
        RelStep(rel_type='REFERENCES', csv_name='rels_REFERENCES.csv',
                properties=['citation'],
                rows=131, loadable=131, cypher=rel_references),
    ],
    expected_nodes_total=569,
    expected_rels_total=691,
)

if __name__ == "__main__":
    run(LOADER)
