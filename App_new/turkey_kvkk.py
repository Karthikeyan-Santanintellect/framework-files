"""Loader — Turkey KVKK.

Source folder : New sources/Turkey KVKK
Source document(s):
    KB 2/Turkey/Kişisel Verilerin Korunması Kanunu (Turkish Personal Data Protection Law - KVKK).pdf

Graph shape   : 12 node labels, 12 relationship types
Expected size : 286 nodes / 308 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/turkey_kvkk.py --dry-run   # validate, write nothing
    python App_new/turkey_kvkk.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'TURKEY_KVKK'
FOLDER = 'Turkey KVKK'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'TURKEY_KVKK'})
SET f.name = 'Turkey KVKK',
    f.jurisdiction = 'Turkey',
    f.folder = 'Turkey KVKK',
    f.source_document = 'KB 2/Turkey/Kişisel Verilerin Korunması Kanunu (Turkish Personal Data Protection Law - KVKK).pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'TURKEY_KVKK'})
MATCH (n:NewSourceNode {framework_id: 'TURKEY_KVKK'})
WHERE NOT (:NewSourceNode {framework_id: 'TURKEY_KVKK'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_DataSubjectRight.csv  (11 rows, key datasubjectright_id)
node_datasubjectright = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.datasubjectright_id})
  SET
      n:DataSubjectRight,
      n.datasubjectright_id = row.datasubjectright_id,
      n.name = row.name,
      n.verbatim_text = row.verbatim_text,
      n.gdpr_reference = row.gdpr_reference,
      n.kvkk_reference = row.kvkk_reference,
      n.comparative_note = row.comparative_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Document.csv  (1 rows, key document_id)
node_document = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.document_id})
  SET
      n:Document,
      n.document_id = row.document_id,
      n.title_tr = row.title_tr,
      n.title_en = row.title_en,
      n.author = row.author,
      n.author_affiliation = row.author_affiliation,
      n.author_email = row.author_email,
      n.author_orcid = row.author_orcid,
      n.journal = row.journal,
      n.volume = row.volume,
      n.issue = row.issue,
      n.pages = row.pages,
      n.year = row.year,
      n.received = row.received,
      n.accepted = row.accepted,
      n.publisher_site = row.publisher_site,
      n.citation = row.citation,
      n.document_kind = row.document_kind,
      n.language = row.language,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Finding.csv  (19 rows, key finding_id)
node_finding = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.finding_id})
  SET
      n:Finding,
      n.finding_id = row.finding_id,
      n.verbatim_text = row.verbatim_text,
      n.section_id = row.section_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_LegalGround.csv  (9 rows, key legalground_id)
node_legalground = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.legalground_id})
  SET
      n:LegalGround,
      n.legalground_id = row.legalground_id,
      n.name = row.name,
      n.enumerator = row.enumerator,
      n.verbatim_definition = row.verbatim_definition,
      n.gdpr_status = row.gdpr_status,
      n.kvkk_status = row.kvkk_status,
      n.comparative_note = row.comparative_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_LegalPrinciple.csv  (9 rows, key legalprinciple_id)
node_legalprinciple = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.legalprinciple_id})
  SET
      n:LegalPrinciple,
      n.legalprinciple_id = row.legalprinciple_id,
      n.name = row.name,
      n.enumerator = row.enumerator,
      n.verbatim_definition = row.verbatim_definition,
      n.comparative_note = row.comparative_note,
      n.kvkk_status = row.kvkk_status,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Measure.csv  (3 rows, key measure_id)
node_measure = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.measure_id})
  SET
      n:Measure,
      n.measure_id = row.measure_id,
      n.verbatim_text = row.verbatim_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Provision.csv  (128 rows, key provision_id)
node_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.provision_id})
  SET
      n:Provision,
      n.provision_id = row.provision_id,
      n.instrument = row.instrument,
      n.article_label = row.article_label,
      n.note = row.note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Reference.csv  (37 rows, key reference_id)
node_reference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.reference_id})
  SET
      n:Reference,
      n.reference_id = row.reference_id,
      n.citation = row.citation,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (11 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.heading = row.heading,
      n.order = row.order,
      n.verbatim_text = row.verbatim_text,
      n.language = row.language,
      n.keywords = row.keywords,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SpecialCategory.csv  (4 rows, key specialcategory_id)
node_specialcategory = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.specialcategory_id})
  SET
      n:SpecialCategory,
      n.specialcategory_id = row.specialcategory_id,
      n.name = row.name,
      n.verbatim_text = row.verbatim_text,
      n.recognised_under = row.recognised_under,
      n.note = row.note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Table.csv  (5 rows, key table_id)
node_table = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.table_id})
  SET
      n:Table,
      n.table_id = row.table_id,
      n.label = row.label,
      n.caption = row.caption,
      n.column_headings = row.column_headings,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_TableRow.csv  (49 rows, key tablerow_id)
node_tablerow = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.tablerow_id})
  SET
      n:TableRow,
      n.tablerow_id = row.tablerow_id,
      n.table_id = row.table_id,
      n.row_order = row.row_order,
      n.col1 = row.col1,
      n.col2 = row.col2,
      n.col3 = row.col3,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_cites_provision.csv -> :CITES_PROVISION  (107 rows)
rel_cites_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES_PROVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:CITES_PROVISION]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_cites_reference.csv -> :CITES_REFERENCE  (37 rows)
rel_cites_reference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES_REFERENCE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:CITES_REFERENCE]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains_table.csv -> :CONTAINS_TABLE  (5 rows)
rel_contains_table = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CONTAINS_TABLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:CONTAINS_TABLE]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines_category.csv -> :DEFINES_CATEGORY  (4 rows)
rel_defines_category = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_CATEGORY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_CATEGORY]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_discusses.csv -> :DISCUSSES  (32 rows)
rel_discusses = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DISCUSSES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:DISCUSSES]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_row.csv -> :HAS_ROW  (49 rows)
rel_has_row = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ROW'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:HAS_ROW]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_section.csv -> :HAS_SECTION  (11 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_related_legal_ground.csv -> :RELATED_LEGAL_GROUND  (4 rows)
rel_related_legal_ground = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'RELATED_LEGAL_GROUND'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:RELATED_LEGAL_GROUND]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_related_principle.csv -> :RELATED_PRINCIPLE  (18 rows)
rel_related_principle = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'RELATED_PRINCIPLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:RELATED_PRINCIPLE]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_row_about.csv -> :ROW_ABOUT  (20 rows)
rel_row_about = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ROW_ABOUT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:ROW_ABOUT]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_states_finding.csv -> :STATES_FINDING  (19 rows)
rel_states_finding = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'STATES_FINDING'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:STATES_FINDING]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_subsection_of.csv -> :SUBSECTION_OF  (2 rows)
rel_subsection_of = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SUBSECTION_OF'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'TURKEY_KVKK', node_id: row.target_id})
  MERGE (s)-[r:SUBSECTION_OF]->(t)
  SET r.detail = row.detail
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Turkey KVKK',
    folder=FOLDER,
    jurisdiction='Turkey',
    source_document='KB 2/Turkey/Kişisel Verilerin Korunması Kanunu (Turkish Personal Data Protection Law - KVKK).pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='DataSubjectRight', csv_name='nodes_DataSubjectRight.csv', id_column='datasubjectright_id',
                 properties=['name', 'verbatim_text', 'gdpr_reference', 'kvkk_reference', 'comparative_note', 'source_document'],
                 rows=11, loadable=11, cypher=node_datasubjectright),
        NodeStep(label='Document', csv_name='nodes_Document.csv', id_column='document_id',
                 properties=['title_tr', 'title_en', 'author', 'author_affiliation', 'author_email', 'author_orcid', 'journal', 'volume', 'issue', 'pages', 'year', 'received', 'accepted', 'publisher_site', 'citation', 'document_kind', 'language', 'source_document'],
                 rows=1, loadable=1, cypher=node_document),
        NodeStep(label='Finding', csv_name='nodes_Finding.csv', id_column='finding_id',
                 properties=['verbatim_text', 'section_id', 'source_document'],
                 rows=19, loadable=19, cypher=node_finding),
        NodeStep(label='LegalGround', csv_name='nodes_LegalGround.csv', id_column='legalground_id',
                 properties=['name', 'enumerator', 'verbatim_definition', 'gdpr_status', 'kvkk_status', 'comparative_note', 'source_document'],
                 rows=9, loadable=9, cypher=node_legalground),
        NodeStep(label='LegalPrinciple', csv_name='nodes_LegalPrinciple.csv', id_column='legalprinciple_id',
                 properties=['name', 'enumerator', 'verbatim_definition', 'comparative_note', 'kvkk_status', 'source_document'],
                 rows=9, loadable=9, cypher=node_legalprinciple),
        NodeStep(label='Measure', csv_name='nodes_Measure.csv', id_column='measure_id',
                 properties=['verbatim_text', 'source_document'],
                 rows=3, loadable=3, cypher=node_measure),
        NodeStep(label='Provision', csv_name='nodes_Provision.csv', id_column='provision_id',
                 properties=['instrument', 'article_label', 'note', 'source_document'],
                 rows=128, loadable=128, cypher=node_provision),
        NodeStep(label='Reference', csv_name='nodes_Reference.csv', id_column='reference_id',
                 properties=['citation', 'source_document'],
                 rows=37, loadable=37, cypher=node_reference),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['heading', 'order', 'verbatim_text', 'language', 'keywords', 'source_document'],
                 rows=11, loadable=11, cypher=node_section),
        NodeStep(label='SpecialCategory', csv_name='nodes_SpecialCategory.csv', id_column='specialcategory_id',
                 properties=['name', 'verbatim_text', 'recognised_under', 'note', 'source_document'],
                 rows=4, loadable=4, cypher=node_specialcategory),
        NodeStep(label='Table', csv_name='nodes_Table.csv', id_column='table_id',
                 properties=['label', 'caption', 'column_headings', 'source_document'],
                 rows=5, loadable=5, cypher=node_table),
        NodeStep(label='TableRow', csv_name='nodes_TableRow.csv', id_column='tablerow_id',
                 properties=['table_id', 'row_order', 'col1', 'col2', 'col3', 'source_document'],
                 rows=49, loadable=49, cypher=node_tablerow),
    ],
    rel_steps=[
        RelStep(rel_type='CITES_PROVISION', csv_name='rels_cites_provision.csv',
                properties=['detail'],
                rows=107, loadable=107, cypher=rel_cites_provision),
        RelStep(rel_type='CITES_REFERENCE', csv_name='rels_cites_reference.csv',
                properties=['detail'],
                rows=37, loadable=37, cypher=rel_cites_reference),
        RelStep(rel_type='CONTAINS_TABLE', csv_name='rels_contains_table.csv',
                properties=['detail'],
                rows=5, loadable=5, cypher=rel_contains_table),
        RelStep(rel_type='DEFINES_CATEGORY', csv_name='rels_defines_category.csv',
                properties=['detail'],
                rows=4, loadable=4, cypher=rel_defines_category),
        RelStep(rel_type='DISCUSSES', csv_name='rels_discusses.csv',
                properties=['detail'],
                rows=32, loadable=32, cypher=rel_discusses),
        RelStep(rel_type='HAS_ROW', csv_name='rels_has_row.csv',
                properties=['detail'],
                rows=49, loadable=49, cypher=rel_has_row),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_has_section.csv',
                properties=['detail'],
                rows=11, loadable=11, cypher=rel_has_section),
        RelStep(rel_type='RELATED_LEGAL_GROUND', csv_name='rels_related_legal_ground.csv',
                properties=['detail'],
                rows=4, loadable=4, cypher=rel_related_legal_ground),
        RelStep(rel_type='RELATED_PRINCIPLE', csv_name='rels_related_principle.csv',
                properties=['detail'],
                rows=18, loadable=18, cypher=rel_related_principle),
        RelStep(rel_type='ROW_ABOUT', csv_name='rels_row_about.csv',
                properties=['detail'],
                rows=20, loadable=20, cypher=rel_row_about),
        RelStep(rel_type='STATES_FINDING', csv_name='rels_states_finding.csv',
                properties=['detail'],
                rows=19, loadable=19, cypher=rel_states_finding),
        RelStep(rel_type='SUBSECTION_OF', csv_name='rels_subsection_of.csv',
                properties=['detail'],
                rows=2, loadable=2, cypher=rel_subsection_of),
    ],
    expected_nodes_total=286,
    expected_rels_total=308,
)

if __name__ == "__main__":
    run(LOADER)
