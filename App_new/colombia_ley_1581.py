"""Loader — Colombia Ley 1581.

Source folder : New sources/Colombia Ley 1581
Source document(s):
    KB 2/South America/Ley_1581_de_2012.pdf

Graph shape   : 14 node labels, 10 relationship types
Expected size : 284 nodes / 402 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/colombia_ley_1581.py --dry-run   # validate, write nothing
    python App_new/colombia_ley_1581.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'COLOMBIA_LEY_1581'
FOLDER = 'Colombia Ley 1581'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'COLOMBIA_LEY_1581'})
SET f.name = 'Colombia Ley 1581',
    f.jurisdiction = 'Colombia',
    f.folder = 'Colombia Ley 1581',
    f.source_document = 'KB 2/South America/Ley_1581_de_2012.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'COLOMBIA_LEY_1581'})
MATCH (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581'})
WHERE NOT (:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Actor.csv  (8 rows, key actor_id)
node_actor = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.actor_id})
  SET
      n:Actor,
      n.actor_id = row.actor_id,
      n.nombre = row.nombre,
      n.rol = row.rol,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Articulo.csv  (30 rows, key articulo_id)
node_articulo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.articulo_id})
  SET
      n:Articulo,
      n.articulo_id = row.articulo_id,
      n.numero = row.numero,
      n.titulo_id = row.titulo_id,
      n.capitulo_id = row.capitulo_id,
      n.epigrafe = row.epigrafe,
      n.texto_encabezado = row.texto_encabezado,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Capitulo.csv  (3 rows, key capitulo_id)
node_capitulo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.capitulo_id})
  SET
      n:Capitulo,
      n.capitulo_id = row.capitulo_id,
      n.titulo_id = row.titulo_id,
      n.numeral = row.numeral,
      n.label = row.label,
      n.heading = row.heading,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clausula.csv  (157 rows, key clausula_id)
node_clausula = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.clausula_id})
  SET
      n:Clausula,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.tipo = row.tipo,
      n.marcador = row.marcador,
      n.orden = row.orden,
      n.texto = row.texto,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_CriterioGraduacion.csv  (6 rows, key criterio_id)
node_criteriograduacion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.criterio_id})
  SET
      n:CriterioGraduacion,
      n.criterio_id = row.criterio_id,
      n.texto = row.texto,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deber.csv  (27 rows, key deber_id)
node_deber = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.deber_id})
  SET
      n:Deber,
      n.deber_id = row.deber_id,
      n.sujeto = row.sujeto,
      n.texto = row.texto,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definicion.csv  (7 rows, key definicion_id)
node_definicion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.definicion_id})
  SET
      n:Definicion,
      n.definicion_id = row.definicion_id,
      n.termino = row.termino,
      n.definicion = row.definicion,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Derecho.csv  (6 rows, key derecho_id)
node_derecho = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.derecho_id})
  SET
      n:Derecho,
      n.derecho_id = row.derecho_id,
      n.texto = row.texto,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Funcion.csv  (11 rows, key funcion_id)
node_funcion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.funcion_id})
  SET
      n:Funcion,
      n.funcion_id = row.funcion_id,
      n.texto = row.texto,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrumento.csv  (1 rows, key instrumento_id)
node_instrumento = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.instrumento_id})
  SET
      n:Instrumento,
      n.instrumento_id = row.instrumento_id,
      n.titulo_oficial = row.titulo_oficial,
      n.fecha = row.fecha,
      n.epigrafe = row.epigrafe,
      n.expedida_por = row.expedida_por,
      n.notas = row.notas,
      n.publicacion = row.publicacion,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Principio.csv  (8 rows, key principio_id)
node_principio = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.principio_id})
  SET
      n:Principio,
      n.principio_id = row.principio_id,
      n.nombre = row.nombre,
      n.texto = row.texto,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ReferenciaExterna.csv  (7 rows, key referencia_id)
node_referenciaexterna = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.referencia_id})
  SET
      n:ReferenciaExterna,
      n.referencia_id = row.referencia_id,
      n.titulo = row.titulo,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Sancion.csv  (4 rows, key sancion_id)
node_sancion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.sancion_id})
  SET
      n:Sancion,
      n.sancion_id = row.sancion_id,
      n.texto = row.texto,
      n.limite_maximo = row.limite_maximo,
      n.plazo = row.plazo,
      n.clausula_id = row.clausula_id,
      n.articulo_id = row.articulo_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Titulo.csv  (9 rows, key titulo_id)
node_titulo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.titulo_id})
  SET
      n:Titulo,
      n.titulo_id = row.titulo_id,
      n.numeral = row.numeral,
      n.label = row.label,
      n.heading = row.heading,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CONTAINS.csv -> :CONTAINS  (199 rows)
rel_contains = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CONTAINS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:CONTAINS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (2 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :DEFINED_IN  (7 rows)
rel_defined_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:DEFINED_IN]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :STATED_IN  (14 rows)
rel_stated_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'STATED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:STATED_IN]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :GRANTED_BY  (6 rows)
rel_granted_by = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'GRANTED_BY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:GRANTED_BY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :IMPOSED_UNDER  (4 rows)
rel_imposed_under = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSED_UNDER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:IMPOSED_UNDER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :ASSIGNED_BY  (11 rows)
rel_assigned_by = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ASSIGNED_BY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:ASSIGNED_BY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DERIVED_FROM.csv -> :IMPOSED_BY  (27 rows)
rel_imposed_by = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSED_BY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:IMPOSED_BY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES.csv -> :REFERENCES  (9 rows)
rel_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERS_TO_ACTOR.csv -> :REFERS_TO_ACTOR  (123 rows)
rel_refers_to_actor = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERS_TO_ACTOR'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'COLOMBIA_LEY_1581', node_id: row.target_id})
  MERGE (s)-[r:REFERS_TO_ACTOR]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Colombia Ley 1581',
    folder=FOLDER,
    jurisdiction='Colombia',
    source_document='KB 2/South America/Ley_1581_de_2012.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Actor', csv_name='nodes_Actor.csv', id_column='actor_id',
                 properties=['nombre', 'rol', 'source_document'],
                 rows=8, loadable=8, cypher=node_actor),
        NodeStep(label='Articulo', csv_name='nodes_Articulo.csv', id_column='articulo_id',
                 properties=['numero', 'titulo_id', 'capitulo_id', 'epigrafe', 'texto_encabezado', 'source_document'],
                 rows=30, loadable=30, cypher=node_articulo),
        NodeStep(label='Capitulo', csv_name='nodes_Capitulo.csv', id_column='capitulo_id',
                 properties=['titulo_id', 'numeral', 'label', 'heading', 'source_document'],
                 rows=3, loadable=3, cypher=node_capitulo),
        NodeStep(label='Clausula', csv_name='nodes_Clausula.csv', id_column='clausula_id',
                 properties=['articulo_id', 'tipo', 'marcador', 'orden', 'texto', 'source_document'],
                 rows=157, loadable=157, cypher=node_clausula),
        NodeStep(label='CriterioGraduacion', csv_name='nodes_CriterioGraduacion.csv', id_column='criterio_id',
                 properties=['texto', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=6, loadable=6, cypher=node_criteriograduacion),
        NodeStep(label='Deber', csv_name='nodes_Deber.csv', id_column='deber_id',
                 properties=['sujeto', 'texto', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=27, loadable=27, cypher=node_deber),
        NodeStep(label='Definicion', csv_name='nodes_Definicion.csv', id_column='definicion_id',
                 properties=['termino', 'definicion', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=7, loadable=7, cypher=node_definicion),
        NodeStep(label='Derecho', csv_name='nodes_Derecho.csv', id_column='derecho_id',
                 properties=['texto', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=6, loadable=6, cypher=node_derecho),
        NodeStep(label='Funcion', csv_name='nodes_Funcion.csv', id_column='funcion_id',
                 properties=['texto', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=11, loadable=11, cypher=node_funcion),
        NodeStep(label='Instrumento', csv_name='nodes_Instrumento.csv', id_column='instrumento_id',
                 properties=['titulo_oficial', 'fecha', 'epigrafe', 'expedida_por', 'notas', 'publicacion', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrumento),
        NodeStep(label='Principio', csv_name='nodes_Principio.csv', id_column='principio_id',
                 properties=['nombre', 'texto', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=8, loadable=8, cypher=node_principio),
        NodeStep(label='ReferenciaExterna', csv_name='nodes_ReferenciaExterna.csv', id_column='referencia_id',
                 properties=['titulo', 'source_document'],
                 rows=7, loadable=7, cypher=node_referenciaexterna),
        NodeStep(label='Sancion', csv_name='nodes_Sancion.csv', id_column='sancion_id',
                 properties=['texto', 'limite_maximo', 'plazo', 'clausula_id', 'articulo_id', 'source_document'],
                 rows=4, loadable=4, cypher=node_sancion),
        NodeStep(label='Titulo', csv_name='nodes_Titulo.csv', id_column='titulo_id',
                 properties=['numeral', 'label', 'heading', 'source_document'],
                 rows=9, loadable=9, cypher=node_titulo),
    ],
    rel_steps=[
        RelStep(rel_type='CONTAINS', csv_name='rels_CONTAINS.csv',
                properties=[],
                rows=199, loadable=199, cypher=rel_contains),
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=[],
                rows=2, loadable=2, cypher=rel_cross_references),
        RelStep(rel_type='DEFINED_IN', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_defined_in),
        RelStep(rel_type='STATED_IN', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=14, loadable=14, cypher=rel_stated_in),
        RelStep(rel_type='GRANTED_BY', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_granted_by),
        RelStep(rel_type='IMPOSED_UNDER', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=4, loadable=4, cypher=rel_imposed_under),
        RelStep(rel_type='ASSIGNED_BY', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=11, loadable=11, cypher=rel_assigned_by),
        RelStep(rel_type='IMPOSED_BY', csv_name='rels_DERIVED_FROM.csv',
                properties=[],
                rows=27, loadable=27, cypher=rel_imposed_by),
        RelStep(rel_type='REFERENCES', csv_name='rels_REFERENCES.csv',
                properties=[],
                rows=9, loadable=9, cypher=rel_references),
        RelStep(rel_type='REFERS_TO_ACTOR', csv_name='rels_REFERS_TO_ACTOR.csv',
                properties=[],
                rows=123, loadable=123, cypher=rel_refers_to_actor),
    ],
    expected_nodes_total=284,
    expected_rels_total=402,
)

if __name__ == "__main__":
    run(LOADER)
