"""Loader — China Data Security Law.

Source folder : New sources/China Data Security Law
Source document(s):
    KB 2/China/Data Security Law of China.pdf

Graph shape   : 10 node labels, 11 relationship types
Expected size : 192 nodes / 210 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/china_data_security_law.py --dry-run   # validate, write nothing
    python App_new/china_data_security_law.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'CHINA_DATA_SECURITY_LAW'
FOLDER = 'China Data Security Law'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'CHINA_DATA_SECURITY_LAW'})
SET f.name = 'China Data Security Law',
    f.jurisdiction = 'China',
    f.folder = 'China Data Security Law',
    f.source_document = 'KB 2/China/Data Security Law of China.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'CHINA_DATA_SECURITY_LAW'})
MATCH (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW'})
WHERE NOT (:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Article.csv  (55 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.article_label = row.article_label,
      n.article_number = row.article_number,
      n.chapter_id = row.chapter_id,
      n.paragraph_count = row.paragraph_count,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Authority.csv  (7 rows, key authority_id)
node_authority = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.authority_id})
  SET
      n:Authority,
      n.authority_id = row.authority_id,
      n.name = row.name,
      n.paragraph_id = row.paragraph_id,
      n.role_text = row.role_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (7 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.chapter_label = row.chapter_label,
      n.chapter_numeral = row.chapter_numeral,
      n.chapter_number = row.chapter_number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DataSecuritySystem.csv  (9 rows, key system_id)
node_datasecuritysystem = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.system_id})
  SET
      n:DataSecuritySystem,
      n.system_id = row.system_id,
      n.name = row.name,
      n.article_id = row.article_id,
      n.paragraph_id = row.paragraph_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (4 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.article_id = row.article_id,
      n.paragraph_id = row.paragraph_id,
      n.definition_text = row.definition_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ExternalInstrument.csv  (2 rows, key external_instrument_id)
node_externalinstrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.external_instrument_id})
  SET
      n:ExternalInstrument,
      n.external_instrument_id = row.external_instrument_id,
      n.name = row.name,
      n.cited_in_paragraph_id = row.cited_in_paragraph_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.title = row.title,
      n.jurisdiction = row.jurisdiction,
      n.promulgator = row.promulgator,
      n.document_number = row.document_number,
      n.adoption_date = row.adoption_date,
      n.promulgation_date = row.promulgation_date,
      n.effective_date = row.effective_date,
      n.chapter_count = row.chapter_count,
      n.article_count = row.article_count,
      n.language = row.language,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Obligation.csv  (17 rows, key obligation_id)
node_obligation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.obligation_id})
  SET
      n:Obligation,
      n.obligation_id = row.obligation_id,
      n.paragraph_id = row.paragraph_id,
      n.article_id = row.article_id,
      n.duty_bearer = row.duty_bearer,
      n.subject = row.subject,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (75 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.article_id = row.article_id,
      n.paragraph_label = row.paragraph_label,
      n.article_number = row.article_number,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (15 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.paragraph_id = row.paragraph_id,
      n.article_id = row.article_id,
      n.violation = row.violation,
      n.sanctions = row.sanctions,
      n.entity_fine_min = row.entity_fine_min,
      n.entity_fine_max = row.entity_fine_max,
      n.individual_fine_min = row.individual_fine_min,
      n.individual_fine_max = row.individual_fine_max,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_assigns_authority.csv -> :ASSIGNS_AUTHORITY  (7 rows)
rel_assigns_authority = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ASSIGNS_AUTHORITY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:ASSIGNS_AUTHORITY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_cites_external_instrument.csv -> :CITES_EXTERNAL_INSTRUMENT  (2 rows)
rel_cites_external_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES_EXTERNAL_INSTRUMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:CITES_EXTERNAL_INSTRUMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_cross_reference.csv -> :CROSS_REFERENCES  (6 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines_term.csv -> :DEFINES_TERM  (4 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
  SET r.term = row.term
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_establishes_system.csv -> :ESTABLISHES_SYSTEM  (9 rows)
rel_establishes_system = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ESTABLISHES_SYSTEM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:ESTABLISHES_SYSTEM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_article.csv -> :HAS_ARTICLE  (55 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_chapter.csv -> :HAS_CHAPTER  (7 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_paragraph.csv -> :HAS_PARAGRAPH  (75 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_imposes_obligation.csv -> :IMPOSES_OBLIGATION  (17 rows)
rel_imposes_obligation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_OBLIGATION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_OBLIGATION]->(t)
  SET r.duty_bearer = row.duty_bearer
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_penalty_for_article.csv -> :PENALTY_FOR_ARTICLE  (13 rows)
rel_penalty_for_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PENALTY_FOR_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:PENALTY_FOR_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_prescribes_penalty.csv -> :PRESCRIBES_PENALTY  (15 rows)
rel_prescribes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PRESCRIBES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_DATA_SECURITY_LAW', node_id: row.target_id})
  MERGE (s)-[r:PRESCRIBES_PENALTY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='China Data Security Law',
    folder=FOLDER,
    jurisdiction='China',
    source_document='KB 2/China/Data Security Law of China.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['article_label', 'article_number', 'chapter_id', 'paragraph_count', 'text', 'source_document'],
                 rows=55, loadable=55, cypher=node_article),
        NodeStep(label='Authority', csv_name='nodes_Authority.csv', id_column='authority_id',
                 properties=['name', 'paragraph_id', 'role_text', 'source_document'],
                 rows=7, loadable=7, cypher=node_authority),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['chapter_label', 'chapter_numeral', 'chapter_number', 'title', 'source_document'],
                 rows=7, loadable=7, cypher=node_chapter),
        NodeStep(label='DataSecuritySystem', csv_name='nodes_DataSecuritySystem.csv', id_column='system_id',
                 properties=['name', 'article_id', 'paragraph_id', 'text', 'source_document'],
                 rows=9, loadable=9, cypher=node_datasecuritysystem),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'article_id', 'paragraph_id', 'definition_text', 'source_document'],
                 rows=4, loadable=4, cypher=node_definition),
        NodeStep(label='ExternalInstrument', csv_name='nodes_ExternalInstrument.csv', id_column='external_instrument_id',
                 properties=['name', 'cited_in_paragraph_id', 'source_document'],
                 rows=2, loadable=2, cypher=node_externalinstrument),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['title', 'jurisdiction', 'promulgator', 'document_number', 'adoption_date', 'promulgation_date', 'effective_date', 'chapter_count', 'article_count', 'language', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='Obligation', csv_name='nodes_Obligation.csv', id_column='obligation_id',
                 properties=['paragraph_id', 'article_id', 'duty_bearer', 'subject', 'text', 'source_document'],
                 rows=17, loadable=17, cypher=node_obligation),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['article_id', 'paragraph_label', 'article_number', 'paragraph_number', 'text', 'source_document'],
                 rows=75, loadable=75, cypher=node_paragraph),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['paragraph_id', 'article_id', 'violation', 'sanctions', 'entity_fine_min', 'entity_fine_max', 'individual_fine_min', 'individual_fine_max', 'text', 'source_document'],
                 rows=15, loadable=15, cypher=node_penalty),
    ],
    rel_steps=[
        RelStep(rel_type='ASSIGNS_AUTHORITY', csv_name='rels_assigns_authority.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_assigns_authority),
        RelStep(rel_type='CITES_EXTERNAL_INSTRUMENT', csv_name='rels_cites_external_instrument.csv',
                properties=[],
                rows=2, loadable=2, cypher=rel_cites_external_instrument),
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_cross_reference.csv',
                properties=['note'],
                rows=6, loadable=6, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_defines_term.csv',
                properties=['term'],
                rows=4, loadable=4, cypher=rel_defines_term),
        RelStep(rel_type='ESTABLISHES_SYSTEM', csv_name='rels_establishes_system.csv',
                properties=[],
                rows=9, loadable=9, cypher=rel_establishes_system),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_has_article.csv',
                properties=[],
                rows=55, loadable=55, cypher=rel_has_article),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_has_chapter.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_has_paragraph.csv',
                properties=[],
                rows=75, loadable=75, cypher=rel_has_paragraph),
        RelStep(rel_type='IMPOSES_OBLIGATION', csv_name='rels_imposes_obligation.csv',
                properties=['duty_bearer'],
                rows=17, loadable=17, cypher=rel_imposes_obligation),
        RelStep(rel_type='PENALTY_FOR_ARTICLE', csv_name='rels_penalty_for_article.csv',
                properties=[],
                rows=13, loadable=13, cypher=rel_penalty_for_article),
        RelStep(rel_type='PRESCRIBES_PENALTY', csv_name='rels_prescribes_penalty.csv',
                properties=[],
                rows=15, loadable=15, cypher=rel_prescribes_penalty),
    ],
    expected_nodes_total=192,
    expected_rels_total=210,
)

if __name__ == "__main__":
    run(LOADER)
