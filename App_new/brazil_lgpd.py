"""Loader — Brazil LGPD.

Source folder : New sources/Brazil LGPD
Source document(s):
    lei-13709-14-agosto-2018-787077-normaatualizada-pl.pdf

Graph shape   : 7 node labels, 7 relationship types
Expected size : 516 nodes / 547 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/brazil_lgpd.py --dry-run   # validate, write nothing
    python App_new/brazil_lgpd.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'BRAZIL_LGPD'
FOLDER = 'Brazil LGPD'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'BRAZIL_LGPD'})
SET f.name = 'Brazil LGPD',
    f.jurisdiction = 'Brazil',
    f.folder = 'Brazil LGPD',
    f.source_document = 'lei-13709-14-agosto-2018-787077-normaatualizada-pl.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'BRAZIL_LGPD'})
MATCH (n:NewSourceNode {framework_id: 'BRAZIL_LGPD'})
WHERE NOT (:NewSourceNode {framework_id: 'BRAZIL_LGPD'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Artigo.csv  (81 rows, key artigo_id)
node_artigo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.artigo_id})
  SET
      n:Artigo,
      n.artigo_id = row.artigo_id,
      n.numero = row.numero,
      n.capitulo_id = row.capitulo_id,
      n.secao_id = row.secao_id,
      n.texto_caput = row.texto_caput,
      n.status = row.status,
      n.nota_alteracao = row.nota_alteracao,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Capitulo.csv  (10 rows, key capitulo_id)
node_capitulo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.capitulo_id})
  SET
      n:Capitulo,
      n.capitulo_id = row.capitulo_id,
      n.numero = row.numero,
      n.titulo = row.titulo,
      n.title_en = row.title_en,
      n.nota_alteracao = row.nota_alteracao,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definicao.csv  (19 rows, key definicao_id)
node_definicao = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.definicao_id})
  SET
      n:Definicao,
      n.definicao_id = row.definicao_id,
      n.termo = row.termo,
      n.texto_definicao = row.texto_definicao,
      n.artigo_id = row.artigo_id,
      n.inciso = row.inciso,
      n.dispositivo_id = row.dispositivo_id,
      n.nota_alteracao = row.nota_alteracao,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Dispositivo.csv  (378 rows, key dispositivo_id)
node_dispositivo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.dispositivo_id})
  SET
      n:Dispositivo,
      n.dispositivo_id = row.dispositivo_id,
      n.artigo_id = row.artigo_id,
      n.tipo = row.tipo,
      n.rotulo = row.rotulo,
      n.texto = row.texto,
      n.status = row.status,
      n.nota_alteracao = row.nota_alteracao,
      n.parent_id = row.parent_id,
      n.parent_type = row.parent_type,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Lei.csv  (1 rows, key lei_id)
node_lei = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.lei_id})
  SET
      n:Lei,
      n.lei_id = row.lei_id,
      n.titulo = row.titulo,
      n.ementa = row.ementa,
      n.nota_ementa = row.nota_ementa,
      n.preambulo = row.preambulo,
      n.data_promulgacao = row.data_promulgacao,
      n.local_data = row.local_data,
      n.jurisdicao = row.jurisdicao,
      n.idioma = row.idioma,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Sancao.csv  (12 rows, key sancao_id)
node_sancao = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.sancao_id})
  SET
      n:Sancao,
      n.sancao_id = row.sancao_id,
      n.inciso = row.inciso,
      n.texto = row.texto,
      n.status = row.status,
      n.artigo_id = row.artigo_id,
      n.dispositivo_id = row.dispositivo_id,
      n.nota_alteracao = row.nota_alteracao,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Secao.csv  (15 rows, key secao_id)
node_secao = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.secao_id})
  SET
      n:Secao,
      n.secao_id = row.secao_id,
      n.capitulo_id = row.capitulo_id,
      n.numero = row.numero,
      n.titulo = row.titulo,
      n.title_en = row.title_en,
      n.nota_alteracao = row.nota_alteracao,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_DEFINES.csv -> :DEFINES  (19 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTIGO.csv -> :HAS_ARTIGO  (81 rows)
rel_has_artigo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTIGO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTIGO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CAPITULO.csv -> :HAS_CAPITULO  (10 rows)
rel_has_capitulo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CAPITULO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:HAS_CAPITULO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DISPOSITIVO.csv -> :HAS_DISPOSITIVO  (378 rows)
rel_has_dispositivo = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DISPOSITIVO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:HAS_DISPOSITIVO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECAO.csv -> :HAS_SECAO  (15 rows)
rel_has_secao = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECAO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECAO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_SANCAO.csv -> :IMPOSES_SANCAO  (12 rows)
rel_imposes_sancao = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_SANCAO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_SANCAO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES.csv -> :REFERENCES  (32 rows)
rel_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BRAZIL_LGPD', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES]->(t)
  SET r.citation = row.citation
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Brazil LGPD',
    folder=FOLDER,
    jurisdiction='Brazil',
    source_document='lei-13709-14-agosto-2018-787077-normaatualizada-pl.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Artigo', csv_name='nodes_Artigo.csv', id_column='artigo_id',
                 properties=['numero', 'capitulo_id', 'secao_id', 'texto_caput', 'status', 'nota_alteracao', 'source_document'],
                 rows=81, loadable=81, cypher=node_artigo),
        NodeStep(label='Capitulo', csv_name='nodes_Capitulo.csv', id_column='capitulo_id',
                 properties=['numero', 'titulo', 'title_en', 'nota_alteracao', 'source_document'],
                 rows=10, loadable=10, cypher=node_capitulo),
        NodeStep(label='Definicao', csv_name='nodes_Definicao.csv', id_column='definicao_id',
                 properties=['termo', 'texto_definicao', 'artigo_id', 'inciso', 'dispositivo_id', 'nota_alteracao', 'source_document'],
                 rows=19, loadable=19, cypher=node_definicao),
        NodeStep(label='Dispositivo', csv_name='nodes_Dispositivo.csv', id_column='dispositivo_id',
                 properties=['artigo_id', 'tipo', 'rotulo', 'texto', 'status', 'nota_alteracao', 'parent_id', 'parent_type', 'source_document'],
                 rows=378, loadable=378, cypher=node_dispositivo),
        NodeStep(label='Lei', csv_name='nodes_Lei.csv', id_column='lei_id',
                 properties=['titulo', 'ementa', 'nota_ementa', 'preambulo', 'data_promulgacao', 'local_data', 'jurisdicao', 'idioma', 'source_document'],
                 rows=1, loadable=1, cypher=node_lei),
        NodeStep(label='Sancao', csv_name='nodes_Sancao.csv', id_column='sancao_id',
                 properties=['inciso', 'texto', 'status', 'artigo_id', 'dispositivo_id', 'nota_alteracao', 'source_document'],
                 rows=12, loadable=12, cypher=node_sancao),
        NodeStep(label='Secao', csv_name='nodes_Secao.csv', id_column='secao_id',
                 properties=['capitulo_id', 'numero', 'titulo', 'title_en', 'nota_alteracao', 'source_document'],
                 rows=15, loadable=15, cypher=node_secao),
    ],
    rel_steps=[
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=19, loadable=19, cypher=rel_defines),
        RelStep(rel_type='HAS_ARTIGO', csv_name='rels_HAS_ARTIGO.csv',
                properties=[],
                rows=81, loadable=81, cypher=rel_has_artigo),
        RelStep(rel_type='HAS_CAPITULO', csv_name='rels_HAS_CAPITULO.csv',
                properties=[],
                rows=10, loadable=10, cypher=rel_has_capitulo),
        RelStep(rel_type='HAS_DISPOSITIVO', csv_name='rels_HAS_DISPOSITIVO.csv',
                properties=[],
                rows=378, loadable=378, cypher=rel_has_dispositivo),
        RelStep(rel_type='HAS_SECAO', csv_name='rels_HAS_SECAO.csv',
                properties=[],
                rows=15, loadable=15, cypher=rel_has_secao),
        RelStep(rel_type='IMPOSES_SANCAO', csv_name='rels_IMPOSES_SANCAO.csv',
                properties=[],
                rows=12, loadable=12, cypher=rel_imposes_sancao),
        RelStep(rel_type='REFERENCES', csv_name='rels_REFERENCES.csv',
                properties=['citation'],
                rows=32, loadable=32, cypher=rel_references),
    ],
    expected_nodes_total=516,
    expected_rels_total=547,
)

if __name__ == "__main__":
    run(LOADER)
