"""Loader — APRA CPS 234.

Source folder : New sources/APRA CPS 234
Source document(s):
    APRA Prudential Standard CPS 234 Information Security (July 2019)

Graph shape   : 10 node labels, 12 relationship types
Expected size : 144 nodes / 161 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/apra_cps_234.py --dry-run   # validate, write nothing
    python App_new/apra_cps_234.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'APRA_CPS_234'
FOLDER = 'APRA CPS 234'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'APRA_CPS_234'})
SET f.name = 'APRA CPS 234',
    f.jurisdiction = 'Australia',
    f.folder = 'APRA CPS 234',
    f.source_document = 'APRA Prudential Standard CPS 234 Information Security (July 2019)',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'APRA_CPS_234'})
MATCH (n:NewSourceNode {framework_id: 'APRA_CPS_234'})
WHERE NOT (:NewSourceNode {framework_id: 'APRA_CPS_234'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Deadline.csv  (6 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.obligation = row.obligation,
      n.period = row.period,
      n.paragraph_id = row.paragraph_id,
      n.text = row.text,
      n.trigger = row.trigger,
      n.notify_party = row.notify_party,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (17 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.text = row.text,
      n.defined_in = row.defined_in,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_EnablingProvision.csv  (5 rows, key enablingprovision_id)
node_enablingprovision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.enablingprovision_id})
  SET
      n:EnablingProvision,
      n.enablingprovision_id = row.enablingprovision_id,
      n.act = row.act,
      n.provision = row.provision,
      n.cited_in = row.cited_in,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Footnote.csv  (14 rows, key footnote_id)
node_footnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.footnote_id})
  SET
      n:Footnote,
      n.footnote_id = row.footnote_id,
      n.number = row.number,
      n.attached_to = row.attached_to,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_KeyRequirement.csv  (4 rows, key keyrequirement_id)
node_keyrequirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.keyrequirement_id})
  SET
      n:KeyRequirement,
      n.keyrequirement_id = row.keyrequirement_id,
      n.order = row.order,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (36 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.number = row.number,
      n.section_id = row.section_id,
      n.text = row.text,
      n.closing_text = row.closing_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_RegulatedEntityType.csv  (5 rows, key regulatedentitytype_id)
node_regulatedentitytype = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.regulatedentitytype_id})
  SET
      n:RegulatedEntityType,
      n.regulatedentitytype_id = row.regulatedentitytype_id,
      n.description = row.description,
      n.defined_in = row.defined_in,
      n.governing_act = row.governing_act,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (15 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.heading = row.heading,
      n.order = row.order,
      n.paragraph_range = row.paragraph_range,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Standard.csv  (1 rows, key standard_id)
node_standard = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.standard_id})
  SET
      n:Standard,
      n.standard_id = row.standard_id,
      n.short_name = row.short_name,
      n.title = row.title,
      n.jurisdiction = row.jurisdiction,
      n.issuing_authority = row.issuing_authority,
      n.version_date = row.version_date,
      n.commencement_date = row.commencement_date,
      n.objectives = row.objectives,
      n.source_pdf = row.source_pdf,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Subparagraph.csv  (41 rows, key subparagraph_id)
node_subparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.subparagraph_id})
  SET
      n:Subparagraph,
      n.subparagraph_id = row.subparagraph_id,
      n.label = row.label,
      n.paragraph_id = row.paragraph_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_applies_to.csv -> :APPLIES_TO  (5 rows)
rel_applies_to = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'APPLIES_TO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:APPLIES_TO]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_cross_reference.csv -> :CROSS_REFERENCES  (7 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines.csv -> :DEFINES  (17 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
  SET r.term = row.term
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_entity_type_defined_in.csv -> :DEFINED_IN  (5 rows)
rel_defined_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:DEFINED_IN]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_footnote.csv -> :HAS_FOOTNOTE  (14 rows)
rel_has_footnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_FOOTNOTE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:HAS_FOOTNOTE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_key_requirement.csv -> :HAS_KEY_REQUIREMENT  (4 rows)
rel_has_key_requirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_KEY_REQUIREMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:HAS_KEY_REQUIREMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_paragraph.csv -> :HAS_PARAGRAPH  (36 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_section.csv -> :HAS_SECTION  (15 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_subparagraph.csv -> :HAS_SUBPARAGRAPH  (41 rows)
rel_has_subparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBPARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBPARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_imposes_deadline.csv -> :IMPOSES_DEADLINE  (6 rows)
rel_imposes_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_DEADLINE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_key_requirement_implemented_by.csv -> :IMPLEMENTED_BY  (6 rows)
rel_implemented_by = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPLEMENTED_BY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:IMPLEMENTED_BY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_made_under.csv -> :MADE_UNDER  (5 rows)
rel_made_under = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'MADE_UNDER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'APRA_CPS_234', node_id: row.target_id})
  MERGE (s)-[r:MADE_UNDER]->(t)
  SET r.provision = row.provision
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='APRA CPS 234',
    folder=FOLDER,
    jurisdiction='Australia',
    source_document='APRA Prudential Standard CPS 234 Information Security (July 2019)',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['obligation', 'period', 'paragraph_id', 'text', 'trigger', 'notify_party', 'source_document'],
                 rows=6, loadable=6, cypher=node_deadline),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'text', 'defined_in', 'source_document'],
                 rows=17, loadable=17, cypher=node_definition),
        NodeStep(label='EnablingProvision', csv_name='nodes_EnablingProvision.csv', id_column='enablingprovision_id',
                 properties=['act', 'provision', 'cited_in', 'source_document'],
                 rows=5, loadable=5, cypher=node_enablingprovision),
        NodeStep(label='Footnote', csv_name='nodes_Footnote.csv', id_column='footnote_id',
                 properties=['number', 'attached_to', 'text', 'source_document'],
                 rows=14, loadable=14, cypher=node_footnote),
        NodeStep(label='KeyRequirement', csv_name='nodes_KeyRequirement.csv', id_column='keyrequirement_id',
                 properties=['order', 'text', 'source_document'],
                 rows=4, loadable=4, cypher=node_keyrequirement),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['number', 'section_id', 'text', 'closing_text', 'source_document'],
                 rows=36, loadable=36, cypher=node_paragraph),
        NodeStep(label='RegulatedEntityType', csv_name='nodes_RegulatedEntityType.csv', id_column='regulatedentitytype_id',
                 properties=['description', 'defined_in', 'governing_act', 'source_document'],
                 rows=5, loadable=5, cypher=node_regulatedentitytype),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['heading', 'order', 'paragraph_range', 'source_document'],
                 rows=15, loadable=15, cypher=node_section),
        NodeStep(label='Standard', csv_name='nodes_Standard.csv', id_column='standard_id',
                 properties=['short_name', 'title', 'jurisdiction', 'issuing_authority', 'version_date', 'commencement_date', 'objectives', 'source_pdf', 'source_document'],
                 rows=1, loadable=1, cypher=node_standard),
        NodeStep(label='Subparagraph', csv_name='nodes_Subparagraph.csv', id_column='subparagraph_id',
                 properties=['label', 'paragraph_id', 'text', 'source_document'],
                 rows=41, loadable=41, cypher=node_subparagraph),
    ],
    rel_steps=[
        RelStep(rel_type='APPLIES_TO', csv_name='rels_applies_to.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_applies_to),
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_cross_reference.csv',
                properties=['note'],
                rows=7, loadable=7, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_defines.csv',
                properties=['term'],
                rows=17, loadable=17, cypher=rel_defines),
        RelStep(rel_type='DEFINED_IN', csv_name='rels_entity_type_defined_in.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_defined_in),
        RelStep(rel_type='HAS_FOOTNOTE', csv_name='rels_has_footnote.csv',
                properties=[],
                rows=14, loadable=14, cypher=rel_has_footnote),
        RelStep(rel_type='HAS_KEY_REQUIREMENT', csv_name='rels_has_key_requirement.csv',
                properties=[],
                rows=4, loadable=4, cypher=rel_has_key_requirement),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_has_paragraph.csv',
                properties=[],
                rows=36, loadable=36, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_has_section.csv',
                properties=[],
                rows=15, loadable=15, cypher=rel_has_section),
        RelStep(rel_type='HAS_SUBPARAGRAPH', csv_name='rels_has_subparagraph.csv',
                properties=[],
                rows=41, loadable=41, cypher=rel_has_subparagraph),
        RelStep(rel_type='IMPOSES_DEADLINE', csv_name='rels_imposes_deadline.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_imposes_deadline),
        RelStep(rel_type='IMPLEMENTED_BY', csv_name='rels_key_requirement_implemented_by.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_implemented_by),
        RelStep(rel_type='MADE_UNDER', csv_name='rels_made_under.csv',
                properties=['provision'],
                rows=5, loadable=5, cypher=rel_made_under),
    ],
    expected_nodes_total=144,
    expected_rels_total=161,
)

if __name__ == "__main__":
    run(LOADER)
