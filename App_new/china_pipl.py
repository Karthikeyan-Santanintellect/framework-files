"""Loader — China PIPL.

Source folder : New sources/China PIPL
Source document(s):
    KB 2/China/Personal Information Protection Law.pdf

Graph shape   : 9 node labels, 9 relationship types
Expected size : 282 nodes / 287 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/china_pipl.py --dry-run   # validate, write nothing
    python App_new/china_pipl.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'CHINA_PIPL'
FOLDER = 'China PIPL'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'CHINA_PIPL'})
SET f.name = 'China PIPL',
    f.jurisdiction = 'China',
    f.folder = 'China PIPL',
    f.source_document = 'KB 2/China/Personal Information Protection Law.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'CHINA_PIPL'})
MATCH (n:NewSourceNode {framework_id: 'CHINA_PIPL'})
WHERE NOT (:NewSourceNode {framework_id: 'CHINA_PIPL'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Article.csv  (74 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.article_number = row.article_number,
      n.chapter_number = row.chapter_number,
      n.section_number = row.section_number,
      n.paragraph_count = row.paragraph_count,
      n.item_count = row.item_count,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (8 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.chapter_number = row.chapter_number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (8 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.defined_in_article = row.defined_in_article,
      n.defined_in_clause = row.defined_in_clause,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Item.csv  (62 rows, key item_id)
node_item = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.item_id})
  SET
      n:Item,
      n.item_id = row.item_id,
      n.article_number = row.article_number,
      n.paragraph_id = row.paragraph_id,
      n.item_label = row.item_label,
      n.item_number = row.item_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Law.csv  (1 rows, key law_id)
node_law = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.law_id})
  SET
      n:Law,
      n.law_id = row.law_id,
      n.title = row.title,
      n.jurisdiction = row.jurisdiction,
      n.order_number = row.order_number,
      n.adopting_body = row.adopting_body,
      n.adoption_date = row.adoption_date,
      n.promulgation_text = row.promulgation_text,
      n.effective_date = row.effective_date,
      n.chapter_count = row.chapter_count,
      n.article_count = row.article_count,
      n.language_of_text = row.language_of_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (106 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.article_number = row.article_number,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (10 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.article_number = row.article_number,
      n.clause_id = row.clause_id,
      n.sanction_text = row.sanction_text,
      n.min_amount = row.min_amount,
      n.max_amount = row.max_amount,
      n.applies_to = row.applies_to,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Right.csv  (10 rows, key right_id)
node_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.right_id})
  SET
      n:Right,
      n.right_id = row.right_id,
      n.right_name = row.right_name,
      n.article_number = row.article_number,
      n.clause_id = row.clause_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (3 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.chapter_number = row.chapter_number,
      n.section_number = row.section_number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (6 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.from_clause_id = row.from_clause_id,
      r.context = row.context
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (8 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
  SET r.clause_id = row.clause_id
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_GRANTS_RIGHT.csv -> :GRANTS_RIGHT  (10 rows)
rel_grants_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'GRANTS_RIGHT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:GRANTS_RIGHT]->(t)
  SET r.clause_id = row.clause_id
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTICLE.csv -> :HAS_ARTICLE  (74 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CHAPTER.csv -> :HAS_CHAPTER  (8 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ITEM.csv -> :HAS_ITEM  (62 rows)
rel_has_item = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ITEM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_ITEM]->(t)
  SET r.item_label = row.item_label
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PARAGRAPH.csv -> :HAS_PARAGRAPH  (106 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
  SET r.paragraph_number = row.paragraph_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (3 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_PENALTY.csv -> :IMPOSES_PENALTY  (10 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CHINA_PIPL', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
  SET r.clause_id = row.clause_id
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='China PIPL',
    folder=FOLDER,
    jurisdiction='China',
    source_document='KB 2/China/Personal Information Protection Law.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['article_number', 'chapter_number', 'section_number', 'paragraph_count', 'item_count', 'source_document'],
                 rows=74, loadable=74, cypher=node_article),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['chapter_number', 'title', 'source_document'],
                 rows=8, loadable=8, cypher=node_chapter),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition_text', 'defined_in_article', 'defined_in_clause', 'source_document'],
                 rows=8, loadable=8, cypher=node_definition),
        NodeStep(label='Item', csv_name='nodes_Item.csv', id_column='item_id',
                 properties=['article_number', 'paragraph_id', 'item_label', 'item_number', 'text', 'source_document'],
                 rows=62, loadable=62, cypher=node_item),
        NodeStep(label='Law', csv_name='nodes_Law.csv', id_column='law_id',
                 properties=['title', 'jurisdiction', 'order_number', 'adopting_body', 'adoption_date', 'promulgation_text', 'effective_date', 'chapter_count', 'article_count', 'language_of_text', 'source_document'],
                 rows=1, loadable=1, cypher=node_law),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['article_number', 'paragraph_number', 'text', 'source_document'],
                 rows=106, loadable=106, cypher=node_paragraph),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['article_number', 'clause_id', 'sanction_text', 'min_amount', 'max_amount', 'applies_to', 'source_document'],
                 rows=10, loadable=10, cypher=node_penalty),
        NodeStep(label='Right', csv_name='nodes_Right.csv', id_column='right_id',
                 properties=['right_name', 'article_number', 'clause_id', 'text', 'source_document'],
                 rows=10, loadable=10, cypher=node_right),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['chapter_number', 'section_number', 'title', 'source_document'],
                 rows=3, loadable=3, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=['from_clause_id', 'context'],
                rows=6, loadable=6, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=['clause_id'],
                rows=8, loadable=8, cypher=rel_defines),
        RelStep(rel_type='GRANTS_RIGHT', csv_name='rels_GRANTS_RIGHT.csv',
                properties=['clause_id'],
                rows=10, loadable=10, cypher=rel_grants_right),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_HAS_ARTICLE.csv',
                properties=[],
                rows=74, loadable=74, cypher=rel_has_article),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_HAS_CHAPTER.csv',
                properties=[],
                rows=8, loadable=8, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_ITEM', csv_name='rels_HAS_ITEM.csv',
                properties=['item_label'],
                rows=62, loadable=62, cypher=rel_has_item),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_HAS_PARAGRAPH.csv',
                properties=['paragraph_number'],
                rows=106, loadable=106, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_has_section),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_IMPOSES_PENALTY.csv',
                properties=['clause_id'],
                rows=10, loadable=10, cypher=rel_imposes_penalty),
    ],
    expected_nodes_total=282,
    expected_rels_total=287,
)

if __name__ == "__main__":
    run(LOADER)
