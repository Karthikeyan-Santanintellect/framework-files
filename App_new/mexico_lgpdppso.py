"""Loader — Mexico LGPDPPSO.

Source folder : New sources/Mexico LGPDPPSO
Source document(s):
    Mexico LGPDPPSO (DOF 20-03-2025) — PDP-Public_03-21-2025_GoogleTranslate.pdf (machine translation)

Graph shape   : 11 node labels, 11 relationship types
Expected size : 700 nodes / 737 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/mexico_lgpdppso.py --dry-run   # validate, write nothing
    python App_new/mexico_lgpdppso.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'MEXICO_LGPDPPSO'
FOLDER = 'Mexico LGPDPPSO'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'MEXICO_LGPDPPSO'})
SET f.name = 'Mexico LGPDPPSO',
    f.jurisdiction = 'Mexico',
    f.folder = 'Mexico LGPDPPSO',
    f.source_document = 'Mexico LGPDPPSO (DOF 20-03-2025) — PDP-Public_03-21-2025_GoogleTranslate.pdf (machine translation)',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'MEXICO_LGPDPPSO'})
MATCH (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO'})
WHERE NOT (:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Article.csv  (137 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.number = row.number,
      n.chapter_id = row.chapter_id,
      n.title_id = row.title_id,
      n.chapeau = row.chapeau,
      n.full_text = row.full_text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (21 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.title_id = row.title_id,
      n.label = row.label,
      n.heading = row.heading,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (452 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.clause_type = row.clause_type,
      n.marker = row.marker,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (32 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition = row.definition,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_EnforcementMeasure.csv  (2 rows, key enforcement_measure_id)
node_enforcementmeasure = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.enforcement_measure_id})
  SET
      n:EnforcementMeasure,
      n.enforcement_measure_id = row.enforcement_measure_id,
      n.marker = row.marker,
      n.measure = row.measure,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.short_name = row.short_name,
      n.official_title = row.official_title,
      n.jurisdiction = row.jurisdiction,
      n.publication = row.publication,
      n.promulgated_by = row.promulgated_by,
      n.text_status = row.text_status,
      n.language_note = row.language_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ReferencedInstrument.csv  (5 rows, key referenced_instrument_id)
node_referencedinstrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.referenced_instrument_id})
  SET
      n:ReferencedInstrument,
      n.referenced_instrument_id = row.referenced_instrument_id,
      n.name = row.name,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Right.csv  (5 rows, key right_id)
node_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.right_id})
  SET
      n:Right,
      n.right_id = row.right_id,
      n.name = row.name,
      n.article_id = row.article_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SanctionGround.csv  (14 rows, key sanction_ground_id)
node_sanctionground = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.sanction_ground_id})
  SET
      n:SanctionGround,
      n.sanction_ground_id = row.sanction_ground_id,
      n.marker = row.marker,
      n.conduct = row.conduct,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Title.csv  (11 rows, key title_id)
node_title = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.title_id})
  SET
      n:Title,
      n.title_id = row.title_id,
      n.label = row.label,
      n.heading = row.heading,
      n.order = row.order,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_TransitoryProvision.csv  (20 rows, key transitory_id)
node_transitoryprovision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.transitory_id})
  SET
      n:TransitoryProvision,
      n.transitory_id = row.transitory_id,
      n.ordinal = row.ordinal,
      n.order = row.order,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (24 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (32 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_ESTABLISHES_RIGHT.csv -> :ESTABLISHES_RIGHT  (5 rows)
rel_establishes_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ESTABLISHES_RIGHT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:ESTABLISHES_RIGHT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTICLE.csv -> :HAS_ARTICLE  (137 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CHAPTER.csv -> :HAS_CHAPTER  (21 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (452 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
  SET r.parent_type = row.parent_type
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_TITLE.csv -> :HAS_TITLE  (11 rows)
rel_has_title = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_TITLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:HAS_TITLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_TRANSITORY.csv -> :HAS_TRANSITORY  (20 rows)
rel_has_transitory = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_TRANSITORY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:HAS_TRANSITORY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IS_ENFORCEMENT_MEASURE.csv -> :IS_ENFORCEMENT_MEASURE  (2 rows)
rel_is_enforcement_measure = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IS_ENFORCEMENT_MEASURE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:IS_ENFORCEMENT_MEASURE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IS_SANCTION_GROUND.csv -> :IS_SANCTION_GROUND  (14 rows)
rel_is_sanction_ground = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IS_SANCTION_GROUND'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:IS_SANCTION_GROUND]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES_INSTRUMENT.csv -> :REFERENCES_INSTRUMENT  (19 rows)
rel_references_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_INSTRUMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MEXICO_LGPDPPSO', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_INSTRUMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Mexico LGPDPPSO',
    folder=FOLDER,
    jurisdiction='Mexico',
    source_document='Mexico LGPDPPSO (DOF 20-03-2025) — PDP-Public_03-21-2025_GoogleTranslate.pdf (machine translation)',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['number', 'chapter_id', 'title_id', 'chapeau', 'full_text', 'source_line', 'source_document'],
                 rows=137, loadable=137, cypher=node_article),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['title_id', 'label', 'heading', 'source_line', 'source_document'],
                 rows=21, loadable=21, cypher=node_chapter),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['article_id', 'clause_type', 'marker', 'text', 'source_line', 'source_document'],
                 rows=452, loadable=452, cypher=node_clause),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition', 'clause_id', 'article_id', 'source_line', 'source_document'],
                 rows=32, loadable=32, cypher=node_definition),
        NodeStep(label='EnforcementMeasure', csv_name='nodes_EnforcementMeasure.csv', id_column='enforcement_measure_id',
                 properties=['marker', 'measure', 'clause_id', 'article_id', 'source_line', 'source_document'],
                 rows=2, loadable=2, cypher=node_enforcementmeasure),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['short_name', 'official_title', 'jurisdiction', 'publication', 'promulgated_by', 'text_status', 'language_note', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='ReferencedInstrument', csv_name='nodes_ReferencedInstrument.csv', id_column='referenced_instrument_id',
                 properties=['name', 'source_document'],
                 rows=5, loadable=5, cypher=node_referencedinstrument),
        NodeStep(label='Right', csv_name='nodes_Right.csv', id_column='right_id',
                 properties=['name', 'article_id', 'text', 'source_document'],
                 rows=5, loadable=5, cypher=node_right),
        NodeStep(label='SanctionGround', csv_name='nodes_SanctionGround.csv', id_column='sanction_ground_id',
                 properties=['marker', 'conduct', 'clause_id', 'article_id', 'source_line', 'source_document'],
                 rows=14, loadable=14, cypher=node_sanctionground),
        NodeStep(label='Title', csv_name='nodes_Title.csv', id_column='title_id',
                 properties=['label', 'heading', 'order', 'source_line', 'source_document'],
                 rows=11, loadable=11, cypher=node_title),
        NodeStep(label='TransitoryProvision', csv_name='nodes_TransitoryProvision.csv', id_column='transitory_id',
                 properties=['ordinal', 'order', 'text', 'source_line', 'source_document'],
                 rows=20, loadable=20, cypher=node_transitoryprovision),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=[],
                rows=24, loadable=24, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=32, loadable=32, cypher=rel_defines),
        RelStep(rel_type='ESTABLISHES_RIGHT', csv_name='rels_ESTABLISHES_RIGHT.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_establishes_right),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_HAS_ARTICLE.csv',
                properties=[],
                rows=137, loadable=137, cypher=rel_has_article),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_HAS_CHAPTER.csv',
                properties=[],
                rows=21, loadable=21, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=['parent_type'],
                rows=452, loadable=452, cypher=rel_has_clause),
        RelStep(rel_type='HAS_TITLE', csv_name='rels_HAS_TITLE.csv',
                properties=[],
                rows=11, loadable=11, cypher=rel_has_title),
        RelStep(rel_type='HAS_TRANSITORY', csv_name='rels_HAS_TRANSITORY.csv',
                properties=[],
                rows=20, loadable=20, cypher=rel_has_transitory),
        RelStep(rel_type='IS_ENFORCEMENT_MEASURE', csv_name='rels_IS_ENFORCEMENT_MEASURE.csv',
                properties=[],
                rows=2, loadable=2, cypher=rel_is_enforcement_measure),
        RelStep(rel_type='IS_SANCTION_GROUND', csv_name='rels_IS_SANCTION_GROUND.csv',
                properties=[],
                rows=14, loadable=14, cypher=rel_is_sanction_ground),
        RelStep(rel_type='REFERENCES_INSTRUMENT', csv_name='rels_REFERENCES_INSTRUMENT.csv',
                properties=[],
                rows=19, loadable=19, cypher=rel_references_instrument),
    ],
    expected_nodes_total=700,
    expected_rels_total=737,
)

if __name__ == "__main__":
    run(LOADER)
