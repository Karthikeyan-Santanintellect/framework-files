"""Loader — South Korea PIPA.

Source folder : New sources/South Korea PIPA
Source document(s):
    KB 2/South Korea/Personal Information Protection Act (PIPA).pdf

Graph shape   : 13 node labels, 13 relationship types
Expected size : 915 nodes / 1,339 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/south_korea_pipa.py --dry-run   # validate, write nothing
    python App_new/south_korea_pipa.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'SOUTH_KOREA_PIPA'
FOLDER = 'South Korea PIPA'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'SOUTH_KOREA_PIPA'})
SET f.name = 'South Korea PIPA',
    f.jurisdiction = 'South Korea',
    f.folder = 'South Korea PIPA',
    f.source_document = 'KB 2/South Korea/Personal Information Protection Act (PIPA).pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'SOUTH_KOREA_PIPA'})
MATCH (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA'})
WHERE NOT (:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Act.csv  (1 rows, key act_id)
node_act = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.act_id})
  SET
      n:Act,
      n.act_id = row.act_id,
      n.title = row.title,
      n.enacting_act_no = row.enacting_act_no,
      n.enacting_date = row.enacting_date,
      n.latest_amendment_act_no = row.latest_amendment_act_no,
      n.latest_amendment_date = row.latest_amendment_date,
      n.last_updated = row.last_updated,
      n.language_note = row.language_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Addendum.csv  (10 rows, key addendum_id)
node_addendum = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.addendum_id})
  SET
      n:Addendum,
      n.addendum_id = row.addendum_id,
      n.act_no = row.act_no,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AddendumArticle.csv  (31 rows, key addendum_article_id)
node_addendumarticle = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.addendum_article_id})
  SET
      n:AddendumArticle,
      n.addendum_article_id = row.addendum_article_id,
      n.addendum_id = row.addendum_id,
      n.article_number = row.article_number,
      n.title = row.title,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Amendment.csv  (10 rows, key amendment_id)
node_amendment = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.amendment_id})
  SET
      n:Amendment,
      n.amendment_id = row.amendment_id,
      n.act_no = row.act_no,
      n.date = row.date,
      n.role = row.role,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Article.csv  (115 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.article_number = row.article_number,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.section_id = row.section_id,
      n.chapeau_text = row.chapeau_text,
      n.is_deleted = row.is_deleted,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (10 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.chapter_number = row.chapter_number,
      n.title = row.title,
      n.heading = row.heading,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DataSubjectRight.csv  (5 rows, key right_id)
node_datasubjectright = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.right_id})
  SET
      n:DataSubjectRight,
      n.right_id = row.right_id,
      n.right_number = row.right_number,
      n.text = row.text,
      n.clause_id = row.clause_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (33 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition_type = row.definition_type,
      n.defined_in = row.defined_in,
      n.definition_text = row.definition_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Item.csv  (17 rows, key item_id)
node_item = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.item_id})
  SET
      n:Item,
      n.item_id = row.item_id,
      n.subparagraph_id = row.subparagraph_id,
      n.article_id = row.article_id,
      n.item_letter = row.item_letter,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (336 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.article_id = row.article_id,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (12 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.article_number = row.article_number,
      n.clause_id = row.clause_id,
      n.sanction_type = row.sanction_type,
      n.max_imprisonment = row.max_imprisonment,
      n.max_monetary = row.max_monetary,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (3 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.section_number = row.section_number,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Subparagraph.csv  (332 rows, key subparagraph_id)
node_subparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.subparagraph_id})
  SET
      n:Subparagraph,
      n.subparagraph_id = row.subparagraph_id,
      n.article_id = row.article_id,
      n.paragraph_id = row.paragraph_id,
      n.subparagraph_number = row.subparagraph_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_AMENDED_BY.csv -> :AMENDED_BY  (251 rows)
rel_amended_by = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'AMENDED_BY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:AMENDED_BY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (184 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (33 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_GRANTS_RIGHT.csv -> :GRANTS_RIGHT  (5 rows)
rel_grants_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'GRANTS_RIGHT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:GRANTS_RIGHT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ADDENDUM.csv -> :HAS_ADDENDUM  (10 rows)
rel_has_addendum = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ADDENDUM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_ADDENDUM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ADDENDUM_ARTICLE.csv -> :HAS_ADDENDUM_ARTICLE  (31 rows)
rel_has_addendum_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ADDENDUM_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_ADDENDUM_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTICLE.csv -> :HAS_ARTICLE  (115 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CHAPTER.csv -> :HAS_CHAPTER  (10 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ITEM.csv -> :HAS_ITEM  (17 rows)
rel_has_item = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ITEM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_ITEM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PARAGRAPH.csv -> :HAS_PARAGRAPH  (336 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (3 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBPARAGRAPH.csv -> :HAS_SUBPARAGRAPH  (332 rows)
rel_has_subparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBPARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBPARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_PENALTY.csv -> :IMPOSES_PENALTY  (12 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOUTH_KOREA_PIPA', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='South Korea PIPA',
    folder=FOLDER,
    jurisdiction='South Korea',
    source_document='KB 2/South Korea/Personal Information Protection Act (PIPA).pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Act', csv_name='nodes_Act.csv', id_column='act_id',
                 properties=['title', 'enacting_act_no', 'enacting_date', 'latest_amendment_act_no', 'latest_amendment_date', 'last_updated', 'language_note', 'source_document'],
                 rows=1, loadable=1, cypher=node_act),
        NodeStep(label='Addendum', csv_name='nodes_Addendum.csv', id_column='addendum_id',
                 properties=['act_no', 'heading', 'text', 'source_document'],
                 rows=10, loadable=10, cypher=node_addendum),
        NodeStep(label='AddendumArticle', csv_name='nodes_AddendumArticle.csv', id_column='addendum_article_id',
                 properties=['addendum_id', 'article_number', 'title', 'text', 'source_document'],
                 rows=31, loadable=31, cypher=node_addendumarticle),
        NodeStep(label='Amendment', csv_name='nodes_Amendment.csv', id_column='amendment_id',
                 properties=['act_no', 'date', 'role', 'source_document'],
                 rows=10, loadable=10, cypher=node_amendment),
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['article_number', 'title', 'chapter_id', 'section_id', 'chapeau_text', 'is_deleted', 'source_document'],
                 rows=115, loadable=115, cypher=node_article),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['chapter_number', 'title', 'heading', 'source_document'],
                 rows=10, loadable=10, cypher=node_chapter),
        NodeStep(label='DataSubjectRight', csv_name='nodes_DataSubjectRight.csv', id_column='right_id',
                 properties=['right_number', 'text', 'clause_id', 'source_document'],
                 rows=5, loadable=5, cypher=node_datasubjectright),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition_type', 'defined_in', 'definition_text', 'source_document'],
                 rows=33, loadable=33, cypher=node_definition),
        NodeStep(label='Item', csv_name='nodes_Item.csv', id_column='item_id',
                 properties=['subparagraph_id', 'article_id', 'item_letter', 'text', 'source_document'],
                 rows=17, loadable=17, cypher=node_item),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['article_id', 'paragraph_number', 'text', 'source_document'],
                 rows=336, loadable=336, cypher=node_paragraph),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['article_number', 'clause_id', 'sanction_type', 'max_imprisonment', 'max_monetary', 'text', 'source_document'],
                 rows=12, loadable=12, cypher=node_penalty),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['section_number', 'title', 'chapter_id', 'source_document'],
                 rows=3, loadable=3, cypher=node_section),
        NodeStep(label='Subparagraph', csv_name='nodes_Subparagraph.csv', id_column='subparagraph_id',
                 properties=['article_id', 'paragraph_id', 'subparagraph_number', 'text', 'source_document'],
                 rows=332, loadable=332, cypher=node_subparagraph),
    ],
    rel_steps=[
        RelStep(rel_type='AMENDED_BY', csv_name='rels_AMENDED_BY.csv',
                properties=[],
                rows=251, loadable=251, cypher=rel_amended_by),
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=[],
                rows=184, loadable=184, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=33, loadable=33, cypher=rel_defines),
        RelStep(rel_type='GRANTS_RIGHT', csv_name='rels_GRANTS_RIGHT.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_grants_right),
        RelStep(rel_type='HAS_ADDENDUM', csv_name='rels_HAS_ADDENDUM.csv',
                properties=[],
                rows=10, loadable=10, cypher=rel_has_addendum),
        RelStep(rel_type='HAS_ADDENDUM_ARTICLE', csv_name='rels_HAS_ADDENDUM_ARTICLE.csv',
                properties=[],
                rows=31, loadable=31, cypher=rel_has_addendum_article),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_HAS_ARTICLE.csv',
                properties=[],
                rows=115, loadable=115, cypher=rel_has_article),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_HAS_CHAPTER.csv',
                properties=[],
                rows=10, loadable=10, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_ITEM', csv_name='rels_HAS_ITEM.csv',
                properties=[],
                rows=17, loadable=17, cypher=rel_has_item),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_HAS_PARAGRAPH.csv',
                properties=[],
                rows=336, loadable=336, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_has_section),
        RelStep(rel_type='HAS_SUBPARAGRAPH', csv_name='rels_HAS_SUBPARAGRAPH.csv',
                properties=[],
                rows=332, loadable=332, cypher=rel_has_subparagraph),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_IMPOSES_PENALTY.csv',
                properties=[],
                rows=12, loadable=12, cypher=rel_imposes_penalty),
    ],
    expected_nodes_total=915,
    expected_rels_total=1339,
)

if __name__ == "__main__":
    run(LOADER)
