"""Loader — Saudi NCA ECC-2 2024.

Source folder : New sources/Saudi NCA ECC-2 2024
Source document(s):
    NCA Essential Cybersecurity Controls (ECC-2:2024), KB 2/Saudi Arabia/ECC--2024-EN.pdf

Graph shape   : 9 node labels, 10 relationship types
Expected size : 364 nodes / 392 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/saudi_nca_ecc_2_2024.py --dry-run   # validate, write nothing
    python App_new/saudi_nca_ecc_2_2024.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'SAUDI_NCA_ECC_2_2024'
FOLDER = 'Saudi NCA ECC-2 2024'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'SAUDI_NCA_ECC_2_2024'})
SET f.name = 'Saudi NCA ECC-2 2024',
    f.jurisdiction = 'Saudi Arabia',
    f.folder = 'Saudi NCA ECC-2 2024',
    f.source_document = 'NCA Essential Cybersecurity Controls (ECC-2:2024), KB 2/Saudi Arabia/ECC--2024-EN.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'SAUDI_NCA_ECC_2_2024'})
MATCH (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024'})
WHERE NOT (:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Abbreviation.csv  (18 rows, key abbreviation_id)
node_abbreviation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.abbreviation_id})
  SET
      n:Abbreviation,
      n.abbreviation_id = row.abbreviation_id,
      n.abbreviation = row.abbreviation,
      n.full_term = row.full_term,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Control.csv  (108 rows, key control_id)
node_control = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.control_id})
  SET
      n:Control,
      n.control_id = row.control_id,
      n.code = row.code,
      n.subdomain = row.subdomain,
      n.main_domain = row.main_domain,
      n.text = row.text,
      n.has_subcontrols = row.has_subcontrols,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Framework.csv  (1 rows, key framework_id)
node_framework = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.framework_id})
  SET
      n:Framework,
      n.csv_framework_id = row.framework_id,
      n.name = row.name,
      n.code = row.code,
      n.version = row.version,
      n.year_of_issuance = row.year_of_issuance,
      n.previous_version = row.previous_version,
      n.issuing_authority = row.issuing_authority,
      n.document_classification = row.document_classification,
      n.tlp = row.tlp,
      n.binding_language = row.binding_language,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_MainDomain.csv  (4 rows, key maindomain_id)
node_maindomain = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.maindomain_id})
  SET
      n:MainDomain,
      n.maindomain_id = row.maindomain_id,
      n.number = row.number,
      n.name = row.name,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (12 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.title = row.title,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SubControl.csv  (92 rows, key subcontrol_id)
node_subcontrol = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.subcontrol_id})
  SET
      n:SubControl,
      n.subcontrol_id = row.subcontrol_id,
      n.code = row.code,
      n.control = row.control,
      n.subdomain = row.subdomain,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Subdomain.csv  (28 rows, key subdomain_id)
node_subdomain = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.subdomain_id})
  SET
      n:Subdomain,
      n.subdomain_id = row.subdomain_id,
      n.code = row.code,
      n.name = row.name,
      n.objective = row.objective,
      n.main_domain = row.main_domain,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Term.csv  (70 rows, key term_id)
node_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.term_id})
  SET
      n:Term,
      n.term_id = row.term_id,
      n.term = row.term,
      n.definition = row.definition,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_UpdateEntry.csv  (31 rows, key updateentry_id)
node_updateentry = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.updateentry_id})
  SET
      n:UpdateEntry,
      n.updateentry_id = row.updateentry_id,
      n.update_type = row.update_type,
      n.section = row.section,
      n.target_reference = row.target_reference,
      n.rationale = row.rationale,
      n.previous_version = row.previous_version,
      n.framework = row.framework,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (5 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.quoted_reference = row.quoted_reference
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES_TERM.csv -> :DEFINES_TERM  (70 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ABBREVIATION.csv -> :HAS_ABBREVIATION  (18 rows)
rel_has_abbreviation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ABBREVIATION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_ABBREVIATION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CONTROL.csv -> :HAS_CONTROL  (108 rows)
rel_has_control = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CONTROL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_CONTROL]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_MAIN_DOMAIN.csv -> :HAS_MAIN_DOMAIN  (4 rows)
rel_has_main_domain = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_MAIN_DOMAIN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_MAIN_DOMAIN]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (12 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBCONTROL.csv -> :HAS_SUBCONTROL  (92 rows)
rel_has_subcontrol = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBCONTROL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBCONTROL]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBDOMAIN.csv -> :HAS_SUBDOMAIN  (28 rows)
rel_has_subdomain = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBDOMAIN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBDOMAIN]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_UPDATE.csv -> :HAS_UPDATE  (31 rows)
rel_has_update = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_UPDATE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:HAS_UPDATE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_UPDATES.csv -> :UPDATES  (24 rows)
rel_updates = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'UPDATES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SAUDI_NCA_ECC_2_2024', node_id: row.target_id})
  MERGE (s)-[r:UPDATES]->(t)
  SET r.update_type = row.update_type
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Saudi NCA ECC-2 2024',
    folder=FOLDER,
    jurisdiction='Saudi Arabia',
    source_document='NCA Essential Cybersecurity Controls (ECC-2:2024), KB 2/Saudi Arabia/ECC--2024-EN.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Abbreviation', csv_name='nodes_Abbreviation.csv', id_column='abbreviation_id',
                 properties=['abbreviation', 'full_term', 'source_document'],
                 rows=18, loadable=18, cypher=node_abbreviation),
        NodeStep(label='Control', csv_name='nodes_Control.csv', id_column='control_id',
                 properties=['code', 'subdomain', 'main_domain', 'text', 'has_subcontrols', 'source_document'],
                 rows=108, loadable=108, cypher=node_control),
        NodeStep(label='Framework', csv_name='nodes_Framework.csv', id_column='framework_id',
                 properties=['name', 'code', 'version', 'year_of_issuance', 'previous_version', 'issuing_authority', 'document_classification', 'tlp', 'binding_language', 'source_document'],
                 rows=1, loadable=1, cypher=node_framework),
        NodeStep(label='MainDomain', csv_name='nodes_MainDomain.csv', id_column='maindomain_id',
                 properties=['number', 'name', 'source_document'],
                 rows=4, loadable=4, cypher=node_maindomain),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['title', 'text', 'source_document'],
                 rows=12, loadable=12, cypher=node_section),
        NodeStep(label='SubControl', csv_name='nodes_SubControl.csv', id_column='subcontrol_id',
                 properties=['code', 'control', 'subdomain', 'text', 'source_document'],
                 rows=92, loadable=92, cypher=node_subcontrol),
        NodeStep(label='Subdomain', csv_name='nodes_Subdomain.csv', id_column='subdomain_id',
                 properties=['code', 'name', 'objective', 'main_domain', 'source_document'],
                 rows=28, loadable=28, cypher=node_subdomain),
        NodeStep(label='Term', csv_name='nodes_Term.csv', id_column='term_id',
                 properties=['term', 'definition', 'source_document'],
                 rows=70, loadable=70, cypher=node_term),
        NodeStep(label='UpdateEntry', csv_name='nodes_UpdateEntry.csv', id_column='updateentry_id',
                 properties=['update_type', 'section', 'target_reference', 'rationale', 'previous_version', 'framework', 'source_document'],
                 rows=31, loadable=31, cypher=node_updateentry),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=['quoted_reference'],
                rows=5, loadable=5, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_DEFINES_TERM.csv',
                properties=[],
                rows=70, loadable=70, cypher=rel_defines_term),
        RelStep(rel_type='HAS_ABBREVIATION', csv_name='rels_HAS_ABBREVIATION.csv',
                properties=[],
                rows=18, loadable=18, cypher=rel_has_abbreviation),
        RelStep(rel_type='HAS_CONTROL', csv_name='rels_HAS_CONTROL.csv',
                properties=[],
                rows=108, loadable=108, cypher=rel_has_control),
        RelStep(rel_type='HAS_MAIN_DOMAIN', csv_name='rels_HAS_MAIN_DOMAIN.csv',
                properties=[],
                rows=4, loadable=4, cypher=rel_has_main_domain),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=12, loadable=12, cypher=rel_has_section),
        RelStep(rel_type='HAS_SUBCONTROL', csv_name='rels_HAS_SUBCONTROL.csv',
                properties=[],
                rows=92, loadable=92, cypher=rel_has_subcontrol),
        RelStep(rel_type='HAS_SUBDOMAIN', csv_name='rels_HAS_SUBDOMAIN.csv',
                properties=[],
                rows=28, loadable=28, cypher=rel_has_subdomain),
        RelStep(rel_type='HAS_UPDATE', csv_name='rels_HAS_UPDATE.csv',
                properties=[],
                rows=31, loadable=31, cypher=rel_has_update),
        RelStep(rel_type='UPDATES', csv_name='rels_UPDATES.csv',
                properties=['update_type'],
                rows=24, loadable=24, cypher=rel_updates),
    ],
    expected_nodes_total=364,
    expected_rels_total=392,
)

if __name__ == "__main__":
    run(LOADER)
