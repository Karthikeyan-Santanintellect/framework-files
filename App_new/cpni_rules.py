"""Loader — CPNI Rules.

Source folder : New sources/CPNI Rules
Source document(s):
    KB 2/USA/Customer Proprietary Network Information Rules.pdf

Graph shape   : 14 node labels, 15 relationship types
Expected size : 279 nodes / 305 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/cpni_rules.py --dry-run   # validate, write nothing
    python App_new/cpni_rules.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'CPNI_RULES'
FOLDER = 'CPNI Rules'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'CPNI_RULES'})
SET f.name = 'CPNI Rules',
    f.jurisdiction = 'United States',
    f.folder = 'CPNI Rules',
    f.source_document = 'KB 2/USA/Customer Proprietary Network Information Rules.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'CPNI_RULES'})
MATCH (n:NewSourceNode {framework_id: 'CPNI_RULES'})
WHERE NOT (:NewSourceNode {framework_id: 'CPNI_RULES'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Appendix.csv  (1 rows, key appendix_id)
node_appendix = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.appendix_id})
  SET
      n:Appendix,
      n.appendix_id = row.appendix_id,
      n.label = row.label,
      n.heading = row.heading,
      n.contains_rule_text = row.contains_rule_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AppendixParagraph.csv  (31 rows, key appendix_paragraph_id)
node_appendixparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.appendix_paragraph_id})
  SET
      n:AppendixParagraph,
      n.appendix_paragraph_id = row.appendix_paragraph_id,
      n.appendix_id = row.appendix_id,
      n.appendix_section_id = row.appendix_section_id,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.footnote_refs = row.footnote_refs,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AppendixSection.csv  (6 rows, key appendix_section_id)
node_appendixsection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.appendix_section_id})
  SET
      n:AppendixSection,
      n.appendix_section_id = row.appendix_section_id,
      n.appendix_id = row.appendix_id,
      n.label = row.label,
      n.heading = row.heading,
      n.order = row.order,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deadline.csv  (7 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.label = row.label,
      n.period = row.period,
      n.text = row.text,
      n.stated_in = row.stated_in,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DefinedTerm.csv  (15 rows, key defined_term_id)
node_definedterm = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.defined_term_id})
  SET
      n:DefinedTerm,
      n.defined_term_id = row.defined_term_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.defined_in = row.defined_in,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ExternalReference.csv  (41 rows, key external_reference_id)
node_externalreference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.external_reference_id})
  SET
      n:ExternalReference,
      n.external_reference_id = row.external_reference_id,
      n.citation = row.citation,
      n.reference_type = row.reference_type,
      n.title = row.title,
      n.quoted_text_in_document = row.quoted_text_in_document,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Footnote.csv  (119 rows, key footnote_id)
node_footnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.footnote_id})
  SET
      n:Footnote,
      n.footnote_id = row.footnote_id,
      n.scope = row.scope,
      n.number = row.number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.name = row.name,
      n.official_title = row.official_title,
      n.proceeding_caption = row.proceeding_caption,
      n.document_number = row.document_number,
      n.docket_numbers = row.docket_numbers,
      n.issuing_body = row.issuing_body,
      n.adopted = row.adopted,
      n.released = row.released,
      n.comment_date = row.comment_date,
      n.reply_comment_date = row.reply_comment_date,
      n.by_line = row.by_line,
      n.signature_block = row.signature_block,
      n.instrument_type = row.instrument_type,
      n.scope_note = row.scope_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_OrderingClause.csv  (2 rows, key ordering_clause_id)
node_orderingclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.ordering_clause_id})
  SET
      n:OrderingClause,
      n.ordering_clause_id = row.ordering_clause_id,
      n.part_id = row.part_id,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.footnote_refs = row.footnote_refs,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (35 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.part_id = row.part_id,
      n.section_id = row.section_id,
      n.paragraph_number = row.paragraph_number,
      n.text = row.text,
      n.footnote_refs = row.footnote_refs,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (6 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.numeral = row.numeral,
      n.heading = row.heading,
      n.order = row.order,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (8 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.part_id = row.part_id,
      n.label = row.label,
      n.heading = row.heading,
      n.order = row.order,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SeparateStatement.csv  (1 rows, key separate_statement_id)
node_separatestatement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.separate_statement_id})
  SET
      n:SeparateStatement,
      n.separate_statement_id = row.separate_statement_id,
      n.title = row.title,
      n.authors = row.authors,
      n.subject = row.subject,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_StatementParagraph.csv  (6 rows, key statement_paragraph_id)
node_statementparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.statement_paragraph_id})
  SET
      n:StatementParagraph,
      n.statement_paragraph_id = row.statement_paragraph_id,
      n.separate_statement_id = row.separate_statement_id,
      n.order = row.order,
      n.text = row.text,
      n.footnote_refs = row.footnote_refs,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_cites.csv -> :CITES  (99 rows)
rel_cites = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:CITES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_PART  (6 rows)
rel_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_PART]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_SECTION  (14 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_PARAGRAPH  (72 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_ORDERING_CLAUSE  (2 rows)
rel_has_ordering_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ORDERING_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_ORDERING_CLAUSE]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_APPENDIX  (1 rows)
rel_has_appendix = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_APPENDIX'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_APPENDIX]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_SEPARATE_STATEMENT  (1 rows)
rel_has_separate_statement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SEPARATE_STATEMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_SEPARATE_STATEMENT]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines.csv -> :DEFINES  (15 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_footnote.csv -> :HAS_FOOTNOTE  (78 rows)
rel_has_footnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_FOOTNOTE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:HAS_FOOTNOTE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_relations.csv -> :IMPLEMENTS  (1 rows)
rel_implements = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPLEMENTS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:IMPLEMENTS]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_relations.csv -> :CLARIFIES  (2 rows)
rel_clarifies = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CLARIFIES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:CLARIFIES]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_relations.csv -> :RESPONDS_TO  (2 rows)
rel_responds_to = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'RESPONDS_TO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:RESPONDS_TO]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_relations.csv -> :VACATES  (1 rows)
rel_vacates = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'VACATES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:VACATES]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_relations.csv -> :ADOPTED_PURSUANT_TO  (4 rows)
rel_adopted_pursuant_to = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ADOPTED_PURSUANT_TO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:ADOPTED_PURSUANT_TO]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_sets_deadline.csv -> :SETS_DEADLINE  (7 rows)
rel_sets_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SETS_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'CPNI_RULES', node_id: row.target_id})
  MERGE (s)-[r:SETS_DEADLINE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='CPNI Rules',
    folder=FOLDER,
    jurisdiction='United States',
    source_document='KB 2/USA/Customer Proprietary Network Information Rules.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Appendix', csv_name='nodes_Appendix.csv', id_column='appendix_id',
                 properties=['label', 'heading', 'contains_rule_text', 'source_document'],
                 rows=1, loadable=1, cypher=node_appendix),
        NodeStep(label='AppendixParagraph', csv_name='nodes_AppendixParagraph.csv', id_column='appendix_paragraph_id',
                 properties=['appendix_id', 'appendix_section_id', 'paragraph_number', 'text', 'footnote_refs', 'source_document'],
                 rows=31, loadable=31, cypher=node_appendixparagraph),
        NodeStep(label='AppendixSection', csv_name='nodes_AppendixSection.csv', id_column='appendix_section_id',
                 properties=['appendix_id', 'label', 'heading', 'order', 'source_document'],
                 rows=6, loadable=6, cypher=node_appendixsection),
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['label', 'period', 'text', 'stated_in', 'source_document'],
                 rows=7, loadable=7, cypher=node_deadline),
        NodeStep(label='DefinedTerm', csv_name='nodes_DefinedTerm.csv', id_column='defined_term_id',
                 properties=['term', 'definition_text', 'defined_in', 'source_document'],
                 rows=15, loadable=15, cypher=node_definedterm),
        NodeStep(label='ExternalReference', csv_name='nodes_ExternalReference.csv', id_column='external_reference_id',
                 properties=['citation', 'reference_type', 'title', 'quoted_text_in_document', 'source_document'],
                 rows=41, loadable=41, cypher=node_externalreference),
        NodeStep(label='Footnote', csv_name='nodes_Footnote.csv', id_column='footnote_id',
                 properties=['scope', 'number', 'text', 'source_document'],
                 rows=119, loadable=119, cypher=node_footnote),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['name', 'official_title', 'proceeding_caption', 'document_number', 'docket_numbers', 'issuing_body', 'adopted', 'released', 'comment_date', 'reply_comment_date', 'by_line', 'signature_block', 'instrument_type', 'scope_note', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='OrderingClause', csv_name='nodes_OrderingClause.csv', id_column='ordering_clause_id',
                 properties=['part_id', 'paragraph_number', 'text', 'footnote_refs', 'source_document'],
                 rows=2, loadable=2, cypher=node_orderingclause),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['part_id', 'section_id', 'paragraph_number', 'text', 'footnote_refs', 'source_document'],
                 rows=35, loadable=35, cypher=node_paragraph),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['numeral', 'heading', 'order', 'source_document'],
                 rows=6, loadable=6, cypher=node_part),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['part_id', 'label', 'heading', 'order', 'source_document'],
                 rows=8, loadable=8, cypher=node_section),
        NodeStep(label='SeparateStatement', csv_name='nodes_SeparateStatement.csv', id_column='separate_statement_id',
                 properties=['title', 'authors', 'subject', 'source_document'],
                 rows=1, loadable=1, cypher=node_separatestatement),
        NodeStep(label='StatementParagraph', csv_name='nodes_StatementParagraph.csv', id_column='statement_paragraph_id',
                 properties=['separate_statement_id', 'order', 'text', 'footnote_refs', 'source_document'],
                 rows=6, loadable=6, cypher=node_statementparagraph),
    ],
    rel_steps=[
        RelStep(rel_type='CITES', csv_name='rels_cites.csv',
                properties=[],
                rows=99, loadable=99, cypher=rel_cites),
        RelStep(rel_type='HAS_PART', csv_name='rels_contains.csv',
                properties=['note'],
                rows=6, loadable=6, cypher=rel_has_part),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_contains.csv',
                properties=['note'],
                rows=14, loadable=14, cypher=rel_has_section),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_contains.csv',
                properties=['note'],
                rows=72, loadable=72, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_ORDERING_CLAUSE', csv_name='rels_contains.csv',
                properties=['note'],
                rows=2, loadable=2, cypher=rel_has_ordering_clause),
        RelStep(rel_type='HAS_APPENDIX', csv_name='rels_contains.csv',
                properties=['note'],
                rows=1, loadable=1, cypher=rel_has_appendix),
        RelStep(rel_type='HAS_SEPARATE_STATEMENT', csv_name='rels_contains.csv',
                properties=['note'],
                rows=1, loadable=1, cypher=rel_has_separate_statement),
        RelStep(rel_type='DEFINES', csv_name='rels_defines.csv',
                properties=[],
                rows=15, loadable=15, cypher=rel_defines),
        RelStep(rel_type='HAS_FOOTNOTE', csv_name='rels_has_footnote.csv',
                properties=[],
                rows=78, loadable=78, cypher=rel_has_footnote),
        RelStep(rel_type='IMPLEMENTS', csv_name='rels_instrument_relations.csv',
                properties=['note'],
                rows=1, loadable=1, cypher=rel_implements),
        RelStep(rel_type='CLARIFIES', csv_name='rels_instrument_relations.csv',
                properties=['note'],
                rows=2, loadable=2, cypher=rel_clarifies),
        RelStep(rel_type='RESPONDS_TO', csv_name='rels_instrument_relations.csv',
                properties=['note'],
                rows=2, loadable=2, cypher=rel_responds_to),
        RelStep(rel_type='VACATES', csv_name='rels_instrument_relations.csv',
                properties=['note'],
                rows=1, loadable=1, cypher=rel_vacates),
        RelStep(rel_type='ADOPTED_PURSUANT_TO', csv_name='rels_instrument_relations.csv',
                properties=['note'],
                rows=4, loadable=4, cypher=rel_adopted_pursuant_to),
        RelStep(rel_type='SETS_DEADLINE', csv_name='rels_sets_deadline.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_sets_deadline),
    ],
    expected_nodes_total=279,
    expected_rels_total=305,
)

if __name__ == "__main__":
    run(LOADER)
