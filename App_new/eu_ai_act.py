"""Loader — EU AI Act.

Source folder : New sources/EU AI Act
Source document(s):
    KB 2/Europe/EU Artificial Intelligence Act.pdf

Graph shape   : 14 node labels, 7 relationship types
Expected size : 1,774 nodes / 2,264 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/eu_ai_act.py --dry-run   # validate, write nothing
    python App_new/eu_ai_act.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'EU_AI_ACT'
FOLDER = 'EU AI Act'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'EU_AI_ACT'})
SET f.name = 'EU AI Act',
    f.jurisdiction = 'European Union',
    f.folder = 'EU AI Act',
    f.source_document = 'KB 2/Europe/EU Artificial Intelligence Act.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'EU_AI_ACT'})
MATCH (n:NewSourceNode {framework_id: 'EU_AI_ACT'})
WHERE NOT (:NewSourceNode {framework_id: 'EU_AI_ACT'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Annex.csv  (13 rows, key annex_id)
node_annex = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.annex_id})
  SET
      n:Annex,
      n.annex_id = row.annex_id,
      n.number = row.number,
      n.title = row.title,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AnnexSection.csv  (6 rows, key annex_section_id)
node_annexsection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.annex_section_id})
  SET
      n:AnnexSection,
      n.annex_section_id = row.annex_section_id,
      n.annex_id = row.annex_id,
      n.label = row.label,
      n.title = row.title,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Article.csv  (113 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.number = row.number,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.section_id = row.section_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (13 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.number = row.number,
      n.title = row.title,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deadline.csv  (39 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.date = row.date,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (68 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.number = row.number,
      n.term = row.term,
      n.definition = row.definition,
      n.definition_body = row.definition_body,
      n.point_id = row.point_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_HighRiskArea.csv  (8 rows, key area_id)
node_highriskarea = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.area_id})
  SET
      n:HighRiskArea,
      n.area_id = row.area_id,
      n.annex_id = row.annex_id,
      n.number = row.number,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (605 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.article_id = row.article_id,
      n.number = row.number,
      n.subparagraph = row.subparagraph,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (6 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.provision_id = row.provision_id,
      n.article_id = row.article_id,
      n.max_fine_eur = row.max_fine_eur,
      n.max_turnover_percent = row.max_turnover_percent,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Point.csv  (698 rows, key point_id)
node_point = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.point_id})
  SET
      n:Point,
      n.point_id = row.point_id,
      n.parent_id = row.parent_id,
      n.label = row.label,
      n.level = row.level,
      n.point_kind = row.point_kind,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ProhibitedPractice.csv  (8 rows, key prohibited_practice_id)
node_prohibitedpractice = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.prohibited_practice_id})
  SET
      n:ProhibitedPractice,
      n.prohibited_practice_id = row.prohibited_practice_id,
      n.label = row.label,
      n.text = row.text,
      n.point_id = row.point_id,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Recital.csv  (180 rows, key recital_id)
node_recital = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.recital_id})
  SET
      n:Recital,
      n.recital_id = row.recital_id,
      n.number = row.number,
      n.text = row.text,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Regulation.csv  (1 rows, key regulation_id)
node_regulation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.regulation_id})
  SET
      n:Regulation,
      n.regulation_id = row.regulation_id,
      n.celex = row.celex,
      n.title = row.title,
      n.short_title = row.short_title,
      n.date_of_adoption = row.date_of_adoption,
      n.date_of_publication = row.date_of_publication,
      n.eli = row.eli,
      n.legal_basis = row.legal_basis,
      n.eea_relevance = row.eea_relevance,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (16 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.chapter_id = row.chapter_id,
      n.number = row.number,
      n.title = row.title,
      n.source_line = row.source_line,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CONTAINS.csv -> :CONTAINS  (1,644 rows)
rel_contains = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CONTAINS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:CONTAINS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (68 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DEADLINE.csv -> :HAS_DEADLINE  (69 rows)
rel_has_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:HAS_DEADLINE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_PENALTY.csv -> :IMPOSES_PENALTY  (6 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_PROHIBITS.csv -> :PROHIBITS  (8 rows)
rel_prohibits = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PROHIBITS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:PROHIBITS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES.csv -> :REFERENCES  (466 rows)
rel_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_SANCTIONS.csv -> :SANCTIONS  (3 rows)
rel_sanctions = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SANCTIONS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EU_AI_ACT', node_id: row.target_id})
  MERGE (s)-[r:SANCTIONS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='EU AI Act',
    folder=FOLDER,
    jurisdiction='European Union',
    source_document='KB 2/Europe/EU Artificial Intelligence Act.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Annex', csv_name='nodes_Annex.csv', id_column='annex_id',
                 properties=['number', 'title', 'source_line', 'source_document'],
                 rows=13, loadable=13, cypher=node_annex),
        NodeStep(label='AnnexSection', csv_name='nodes_AnnexSection.csv', id_column='annex_section_id',
                 properties=['annex_id', 'label', 'title', 'source_line', 'source_document'],
                 rows=6, loadable=6, cypher=node_annexsection),
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['number', 'title', 'chapter_id', 'section_id', 'source_line', 'source_document'],
                 rows=113, loadable=113, cypher=node_article),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['number', 'title', 'source_line', 'source_document'],
                 rows=13, loadable=13, cypher=node_chapter),
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['date', 'source_document'],
                 rows=39, loadable=39, cypher=node_deadline),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['number', 'term', 'definition', 'definition_body', 'point_id', 'source_line', 'source_document'],
                 rows=68, loadable=68, cypher=node_definition),
        NodeStep(label='HighRiskArea', csv_name='nodes_HighRiskArea.csv', id_column='area_id',
                 properties=['annex_id', 'number', 'text', 'source_line', 'source_document'],
                 rows=8, loadable=8, cypher=node_highriskarea),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['article_id', 'number', 'subparagraph', 'text', 'source_line', 'source_document'],
                 rows=605, loadable=605, cypher=node_paragraph),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['provision_id', 'article_id', 'max_fine_eur', 'max_turnover_percent', 'text', 'source_line', 'source_document'],
                 rows=6, loadable=6, cypher=node_penalty),
        NodeStep(label='Point', csv_name='nodes_Point.csv', id_column='point_id',
                 properties=['parent_id', 'label', 'level', 'point_kind', 'text', 'source_line', 'source_document'],
                 rows=698, loadable=698, cypher=node_point),
        NodeStep(label='ProhibitedPractice', csv_name='nodes_ProhibitedPractice.csv', id_column='prohibited_practice_id',
                 properties=['label', 'text', 'point_id', 'source_line', 'source_document'],
                 rows=8, loadable=8, cypher=node_prohibitedpractice),
        NodeStep(label='Recital', csv_name='nodes_Recital.csv', id_column='recital_id',
                 properties=['number', 'text', 'source_line', 'source_document'],
                 rows=180, loadable=180, cypher=node_recital),
        NodeStep(label='Regulation', csv_name='nodes_Regulation.csv', id_column='regulation_id',
                 properties=['celex', 'title', 'short_title', 'date_of_adoption', 'date_of_publication', 'eli', 'legal_basis', 'eea_relevance', 'source_document'],
                 rows=1, loadable=1, cypher=node_regulation),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['chapter_id', 'number', 'title', 'source_line', 'source_document'],
                 rows=16, loadable=16, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='CONTAINS', csv_name='rels_CONTAINS.csv',
                properties=[],
                rows=1644, loadable=1644, cypher=rel_contains),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=68, loadable=68, cypher=rel_defines),
        RelStep(rel_type='HAS_DEADLINE', csv_name='rels_HAS_DEADLINE.csv',
                properties=[],
                rows=69, loadable=69, cypher=rel_has_deadline),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_IMPOSES_PENALTY.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_imposes_penalty),
        RelStep(rel_type='PROHIBITS', csv_name='rels_PROHIBITS.csv',
                properties=[],
                rows=8, loadable=8, cypher=rel_prohibits),
        RelStep(rel_type='REFERENCES', csv_name='rels_REFERENCES.csv',
                properties=[],
                rows=466, loadable=466, cypher=rel_references),
        RelStep(rel_type='SANCTIONS', csv_name='rels_SANCTIONS.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_sanctions),
    ],
    expected_nodes_total=1766,
    expected_rels_total=2264,
)

if __name__ == "__main__":
    run(LOADER)
