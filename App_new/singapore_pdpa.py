"""Loader — Singapore PDPA.

Source folder : New sources/Singapore PDPA
Source document(s):
    Personal Data Protection Act 2012 (2020 Revised Edition) — KB 2/Singapore/Personal Data Protection Act (PDPA).pdf

Graph shape   : 13 node labels, 13 relationship types
Expected size : 1,392 nodes / 1,597 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/singapore_pdpa.py --dry-run   # validate, write nothing
    python App_new/singapore_pdpa.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'SINGAPORE_PDPA'
FOLDER = 'Singapore PDPA'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'SINGAPORE_PDPA'})
SET f.name = 'Singapore PDPA',
    f.jurisdiction = 'Singapore',
    f.folder = 'Singapore PDPA',
    f.source_document = 'Personal Data Protection Act 2012 (2020 Revised Edition) — KB 2/Singapore/Personal Data Protection Act (PDPA).pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'SINGAPORE_PDPA'})
MATCH (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA'})
WHERE NOT (:NewSourceNode {framework_id: 'SINGAPORE_PDPA'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Act.csv  (1 rows, key act_id)
node_act = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.act_id})
  SET
      n:Act,
      n.act_id = row.act_id,
      n.citation = row.citation,
      n.edition = row.edition,
      n.revision_note = row.revision_note,
      n.consolidation_note = row.consolidation_note,
      n.long_title = row.long_title,
      n.commencement = row.commencement,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Amendment.csv  (9 rows, key amendment_id)
node_amendment = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.amendment_id})
  SET
      n:Amendment,
      n.amendment_id = row.amendment_id,
      n.entry_number = row.entry_number,
      n.citation = row.citation,
      n.details = row.details,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (1,035 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.section_id = row.section_id,
      n.parent_id = row.parent_id,
      n.clause_type = row.clause_type,
      n.label = row.label,
      n.number = row.number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deadline.csv  (14 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.period_text = row.period_text,
      n.provision = row.provision,
      n.provision_id = row.provision_id,
      n.verbatim_text = row.verbatim_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (108 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.scope = row.scope,
      n.defined_in = row.defined_in,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Division.csv  (5 rows, key division_id)
node_division = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.division_id})
  SET
      n:Division,
      n.division_id = row.division_id,
      n.part_id = row.part_id,
      n.number = row.number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (15 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.number = row.number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (23 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.penalty_type = row.penalty_type,
      n.imposed_by = row.imposed_by,
      n.max_fine = row.max_fine,
      n.max_imprisonment = row.max_imprisonment,
      n.turnover_cap = row.turnover_cap,
      n.turnover_threshold = row.turnover_threshold,
      n.continuing_offence_fine = row.continuing_offence_fine,
      n.provision = row.provision,
      n.provision_id = row.provision_id,
      n.verbatim_text = row.verbatim_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Schedule.csv  (11 rows, key schedule_id)
node_schedule = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.schedule_id})
  SET
      n:Schedule,
      n.schedule_id = row.schedule_id,
      n.name = row.name,
      n.ordinal = row.ordinal,
      n.authority = row.authority,
      n.title = row.title,
      n.status = row.status,
      n.note = row.note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ScheduleDivision.csv  (5 rows, key scheduledivision_id)
node_scheduledivision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.scheduledivision_id})
  SET
      n:ScheduleDivision,
      n.scheduledivision_id = row.scheduledivision_id,
      n.schedule_id = row.schedule_id,
      n.schedulepart_id = row.schedulepart_id,
      n.number = row.number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ScheduleParagraph.csv  (63 rows, key scheduleparagraph_id)
node_scheduleparagraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.scheduleparagraph_id})
  SET
      n:ScheduleParagraph,
      n.scheduleparagraph_id = row.scheduleparagraph_id,
      n.schedule_id = row.schedule_id,
      n.parent_id = row.parent_id,
      n.number = row.number,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SchedulePart.csv  (8 rows, key schedulepart_id)
node_schedulepart = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.schedulepart_id})
  SET
      n:SchedulePart,
      n.schedulepart_id = row.schedulepart_id,
      n.schedule_id = row.schedule_id,
      n.number = row.number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (95 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.part_id = row.part_id,
      n.division_id = row.division_id,
      n.number = row.number,
      n.heading = row.heading,
      n.lead_text = row.lead_text,
      n.status = row.status,
      n.repealed_by = row.repealed_by,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_AMENDS.csv -> :AMENDS  (9 rows)
rel_amends = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'AMENDS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:AMENDS]->(t)
  SET r.citation = row.citation
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (108 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
  SET r.term = row.term
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (1,035 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
  SET r.clause_type = row.clause_type
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DEADLINE.csv -> :HAS_DEADLINE  (14 rows)
rel_has_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_DEADLINE]->(t)
  SET r.period_text = row.period_text
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DIVISION.csv -> :HAS_DIVISION  (5 rows)
rel_has_division = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DIVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_DIVISION]->(t)
  SET r.division_number = row.division_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PARAGRAPH.csv -> :HAS_PARAGRAPH  (63 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
  SET r.paragraph_number = row.paragraph_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PART.csv -> :HAS_PART  (15 rows)
rel_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_PART]->(t)
  SET r.part_number = row.part_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SCHEDULE.csv -> :HAS_SCHEDULE  (11 rows)
rel_has_schedule = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SCHEDULE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SCHEDULE]->(t)
  SET r.ordinal = row.ordinal
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (95 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.section_number = row.section_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_PENALTY.csv -> :IMPOSES_PENALTY  (23 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
  SET r.penalty_type = row.penalty_type
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES.csv -> :REFERENCES  (206 rows)
rel_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES]->(t)
  SET r.ref_kind = row.ref_kind,
      r.ref_text = row.ref_text
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_SCHEDULE_HAS_DIVISION.csv -> :SCHEDULE_HAS_DIVISION  (5 rows)
rel_schedule_has_division = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SCHEDULE_HAS_DIVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:SCHEDULE_HAS_DIVISION]->(t)
  SET r.division_number = row.division_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_SCHEDULE_HAS_PART.csv -> :SCHEDULE_HAS_PART  (8 rows)
rel_schedule_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SCHEDULE_HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SINGAPORE_PDPA', node_id: row.target_id})
  MERGE (s)-[r:SCHEDULE_HAS_PART]->(t)
  SET r.part_number = row.part_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Singapore PDPA',
    folder=FOLDER,
    jurisdiction='Singapore',
    source_document='Personal Data Protection Act 2012 (2020 Revised Edition) — KB 2/Singapore/Personal Data Protection Act (PDPA).pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Act', csv_name='nodes_Act.csv', id_column='act_id',
                 properties=['citation', 'edition', 'revision_note', 'consolidation_note', 'long_title', 'commencement', 'source_document'],
                 rows=1, loadable=1, cypher=node_act),
        NodeStep(label='Amendment', csv_name='nodes_Amendment.csv', id_column='amendment_id',
                 properties=['entry_number', 'citation', 'details', 'source_document'],
                 rows=9, loadable=9, cypher=node_amendment),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['section_id', 'parent_id', 'clause_type', 'label', 'number', 'text', 'source_document'],
                 rows=1035, loadable=1035, cypher=node_clause),
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['period_text', 'provision', 'provision_id', 'verbatim_text', 'source_document'],
                 rows=14, loadable=14, cypher=node_deadline),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition_text', 'scope', 'defined_in', 'source_document'],
                 rows=108, loadable=108, cypher=node_definition),
        NodeStep(label='Division', csv_name='nodes_Division.csv', id_column='division_id',
                 properties=['part_id', 'number', 'title', 'source_document'],
                 rows=5, loadable=5, cypher=node_division),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['number', 'title', 'source_document'],
                 rows=15, loadable=15, cypher=node_part),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['penalty_type', 'imposed_by', 'max_fine', 'max_imprisonment', 'turnover_cap', 'turnover_threshold', 'continuing_offence_fine', 'provision', 'provision_id', 'verbatim_text', 'source_document'],
                 rows=23, loadable=23, cypher=node_penalty),
        NodeStep(label='Schedule', csv_name='nodes_Schedule.csv', id_column='schedule_id',
                 properties=['name', 'ordinal', 'authority', 'title', 'status', 'note', 'source_document'],
                 rows=11, loadable=11, cypher=node_schedule),
        NodeStep(label='ScheduleDivision', csv_name='nodes_ScheduleDivision.csv', id_column='scheduledivision_id',
                 properties=['schedule_id', 'schedulepart_id', 'number', 'title', 'source_document'],
                 rows=5, loadable=5, cypher=node_scheduledivision),
        NodeStep(label='ScheduleParagraph', csv_name='nodes_ScheduleParagraph.csv', id_column='scheduleparagraph_id',
                 properties=['schedule_id', 'parent_id', 'number', 'heading', 'text', 'source_document'],
                 rows=63, loadable=63, cypher=node_scheduleparagraph),
        NodeStep(label='SchedulePart', csv_name='nodes_SchedulePart.csv', id_column='schedulepart_id',
                 properties=['schedule_id', 'number', 'title', 'source_document'],
                 rows=8, loadable=8, cypher=node_schedulepart),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['part_id', 'division_id', 'number', 'heading', 'lead_text', 'status', 'repealed_by', 'source_document'],
                 rows=95, loadable=95, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='AMENDS', csv_name='rels_AMENDS.csv',
                properties=['citation'],
                rows=9, loadable=9, cypher=rel_amends),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=['term'],
                rows=108, loadable=108, cypher=rel_defines),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=['clause_type'],
                rows=1035, loadable=1035, cypher=rel_has_clause),
        RelStep(rel_type='HAS_DEADLINE', csv_name='rels_HAS_DEADLINE.csv',
                properties=['period_text'],
                rows=14, loadable=14, cypher=rel_has_deadline),
        RelStep(rel_type='HAS_DIVISION', csv_name='rels_HAS_DIVISION.csv',
                properties=['division_number'],
                rows=5, loadable=5, cypher=rel_has_division),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_HAS_PARAGRAPH.csv',
                properties=['paragraph_number'],
                rows=63, loadable=63, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_PART', csv_name='rels_HAS_PART.csv',
                properties=['part_number'],
                rows=15, loadable=15, cypher=rel_has_part),
        RelStep(rel_type='HAS_SCHEDULE', csv_name='rels_HAS_SCHEDULE.csv',
                properties=['ordinal'],
                rows=11, loadable=11, cypher=rel_has_schedule),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=['section_number'],
                rows=95, loadable=95, cypher=rel_has_section),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_IMPOSES_PENALTY.csv',
                properties=['penalty_type'],
                rows=23, loadable=23, cypher=rel_imposes_penalty),
        RelStep(rel_type='REFERENCES', csv_name='rels_REFERENCES.csv',
                properties=['ref_kind', 'ref_text'],
                rows=206, loadable=206, cypher=rel_references),
        RelStep(rel_type='SCHEDULE_HAS_DIVISION', csv_name='rels_SCHEDULE_HAS_DIVISION.csv',
                properties=['division_number'],
                rows=5, loadable=5, cypher=rel_schedule_has_division),
        RelStep(rel_type='SCHEDULE_HAS_PART', csv_name='rels_SCHEDULE_HAS_PART.csv',
                properties=['part_number'],
                rows=8, loadable=8, cypher=rel_schedule_has_part),
    ],
    expected_nodes_total=1392,
    expected_rels_total=1597,
)

if __name__ == "__main__":
    run(LOADER)
