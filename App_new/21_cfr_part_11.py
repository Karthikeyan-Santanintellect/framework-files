"""Loader — 21 CFR Part 11.

Source folder : New sources/21 CFR Part 11
Source document(s):
    21 CFR Part 11 - Electronic Records; Electronic Signatures (eCFR, up to date as of 5/21/2026); KB 2/USA/21 CFR Part 11.pdf
    Guidance for Industry - Part 11, Electronic Records; Electronic Signatures - Scope and Application (FDA, August 2003); KB 2/USA/Part-11--Electronic-Records--Electronic-Signatures.pdf

Graph shape   : 12 node labels, 20 relationship types
Expected size : 152 nodes / 198 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/21_cfr_part_11.py --dry-run   # validate, write nothing
    python App_new/21_cfr_part_11.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = '21_CFR_PART_11'
FOLDER = '21 CFR Part 11'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: '21_CFR_PART_11'})
SET f.name = '21 CFR Part 11',
    f.jurisdiction = 'United States',
    f.folder = '21 CFR Part 11',
    f.source_document = '21 CFR Part 11 - Electronic Records; Electronic Signatures (eCFR, up to date as of 5/21/2026); KB 2/USA/21 CFR Part 11.pdf | Guidance for Industry - Part 11, Electronic Records; Electronic Signatures - Scope and Application (FDA, August 2003); KB 2/USA/Part-11--Electronic-Records--Electronic-Signatures.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: '21_CFR_PART_11'})
MATCH (n:NewSourceNode {framework_id: '21_CFR_PART_11'})
WHERE NOT (:NewSourceNode {framework_id: '21_CFR_PART_11'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Clause.csv  (66 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.section_id = row.section_id,
      n.parent_clause_id = row.parent_clause_id,
      n.citation = row.citation,
      n.label = row.label,
      n.level = row.level,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (9 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.citation = row.citation,
      n.clause_id = row.clause_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_EnforcementPosition.csv  (8 rows, key enforcementposition_id)
node_enforcementposition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.enforcementposition_id})
  SET
      n:EnforcementPosition,
      n.enforcementposition_id = row.enforcementposition_id,
      n.title = row.title,
      n.position_type = row.position_type,
      n.guidancesection_id = row.guidancesection_id,
      n.statement = row.statement,
      n.caveat = row.caveat,
      n.criteria = row.criteria,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_ExternalReference.csv  (14 rows, key externalreference_id)
node_externalreference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.externalreference_id})
  SET
      n:ExternalReference,
      n.externalreference_id = row.externalreference_id,
      n.citation = row.citation,
      n.reference_type = row.reference_type,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_GuidanceFootnote.csv  (8 rows, key guidancefootnote_id)
node_guidancefootnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.guidancefootnote_id})
  SET
      n:GuidanceFootnote,
      n.guidancefootnote_id = row.guidancefootnote_id,
      n.number = row.number,
      n.guidancesection_id = row.guidancesection_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_GuidanceSection.csv  (14 rows, key guidancesection_id)
node_guidancesection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.guidancesection_id})
  SET
      n:GuidanceSection,
      n.guidancesection_id = row.guidancesection_id,
      n.number = row.number,
      n.heading = row.heading,
      n.parent_id = row.parent_id,
      n.level = row.level,
      n.text = row.text,
      n.instrument_id = row.instrument_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_GuidanceTerm.csv  (5 rows, key guidanceterm_id)
node_guidanceterm = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.guidanceterm_id})
  SET
      n:GuidanceTerm,
      n.guidanceterm_id = row.guidanceterm_id,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.guidancesection_id = row.guidancesection_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (2 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.title = row.title,
      n.instrument_type = row.instrument_type,
      n.issuing_body = row.issuing_body,
      n.authority = row.authority,
      n.source_note = row.source_note,
      n.currency_date = row.currency_date,
      n.status_note = row.status_note,
      n.binding_status = row.binding_status,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Reference.csv  (7 rows, key reference_id)
node_reference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.reference_id})
  SET
      n:Reference,
      n.reference_id = row.reference_id,
      n.category = row.category,
      n.citation = row.citation,
      n.url = row.url,
      n.guidancesection_id = row.guidancesection_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (10 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.number = row.number,
      n.heading = row.heading,
      n.subpart_id = row.subpart_id,
      n.undesignated_text = row.undesignated_text,
      n.amendment_note = row.amendment_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Subpart.csv  (3 rows, key subpart_id)
node_subpart = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.subpart_id})
  SET
      n:Subpart,
      n.subpart_id = row.subpart_id,
      n.designation = row.designation,
      n.heading = row.heading,
      n.instrument_id = row.instrument_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_WithdrawnDocument.csv  (6 rows, key withdrawndocument_id)
node_withdrawndocument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.withdrawndocument_id})
  SET
      n:WithdrawnDocument,
      n.withdrawndocument_id = row.withdrawndocument_id,
      n.title = row.title,
      n.document_type = row.document_type,
      n.withdrawal_statement = row.withdrawal_statement,
      n.federal_register_citation = row.federal_register_citation,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_contains.csv -> :HAS_SUBPART  (3 rows)
rel_has_subpart = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBPART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBPART]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_SECTION  (10 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_CLAUSE  (43 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_contains.csv -> :HAS_SUBCLAUSE  (23 rows)
rel_has_subclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBCLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBCLAUSE]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_cross_references.csv -> :CROSS_REFERENCES  (5 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.cited_as = row.cited_as,
      r.quoted_context = row.quoted_context,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines.csv -> :DEFINES  (9 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
  SET r.citation = row.citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_enforcement_applies_to.csv -> :DISCRETION_APPLIES_TO  (10 rows)
rel_discretion_applies_to = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DISCRETION_APPLIES_TO'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:DISCRETION_APPLIES_TO]->(t)
  SET r.stated_citation = row.stated_citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_enforcement_applies_to.csv -> :INTERPRETS  (2 rows)
rel_interprets = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'INTERPRETS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:INTERPRETS]->(t)
  SET r.stated_citation = row.stated_citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_enforcement_applies_to.csv -> :CONTINUES_TO_ENFORCE  (8 rows)
rel_continues_to_enforce = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CONTINUES_TO_ENFORCE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:CONTINUES_TO_ENFORCE]->(t)
  SET r.stated_citation = row.stated_citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_external_references.csv -> :REFERS_TO_EXTERNAL  (14 rows)
rel_refers_to_external = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERS_TO_EXTERNAL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:REFERS_TO_EXTERNAL]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_discusses.csv -> :DISCUSSES  (21 rows)
rel_discusses = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DISCUSSES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:DISCUSSES]->(t)
  SET r.stated_citation = row.stated_citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_discusses.csv -> :REFERS_TO_SECTION  (1 rows)
rel_refers_to_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERS_TO_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:REFERS_TO_SECTION]->(t)
  SET r.stated_citation = row.stated_citation,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :HAS_SECTION  (4 rows)
rel_has_section_2 = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :HAS_SUBSECTION  (10 rows)
rel_has_subsection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBSECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBSECTION]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :HAS_FOOTNOTE  (8 rows)
rel_has_footnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_FOOTNOTE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:HAS_FOOTNOTE]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :DEFINES_TERM  (5 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :CITES_REFERENCE  (7 rows)
rel_cites_reference = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES_REFERENCE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:CITES_REFERENCE]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_guidance_structure.csv -> :ANNOUNCES  (8 rows)
rel_announces = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ANNOUNCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:ANNOUNCES]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_instrument_links.csv -> :PROVIDES_GUIDANCE_ON  (1 rows)
rel_provides_guidance_on = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PROVIDES_GUIDANCE_ON'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:PROVIDES_GUIDANCE_ON]->(t)
  SET r.basis = row.basis,
      r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_withdrawn_by.csv -> :ANNOUNCES_WITHDRAWAL_OF  (6 rows)
rel_announces_withdrawal_of = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ANNOUNCES_WITHDRAWAL_OF'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: '21_CFR_PART_11', node_id: row.target_id})
  MERGE (s)-[r:ANNOUNCES_WITHDRAWAL_OF]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='21 CFR Part 11',
    folder=FOLDER,
    jurisdiction='United States',
    source_document='21 CFR Part 11 - Electronic Records; Electronic Signatures (eCFR, up to date as of 5/21/2026); KB 2/USA/21 CFR Part 11.pdf | Guidance for Industry - Part 11, Electronic Records; Electronic Signatures - Scope and Application (FDA, August 2003); KB 2/USA/Part-11--Electronic-Records--Electronic-Signatures.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['section_id', 'parent_clause_id', 'citation', 'label', 'level', 'text', 'source_document'],
                 rows=66, loadable=66, cypher=node_clause),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition_text', 'citation', 'clause_id', 'source_document'],
                 rows=9, loadable=9, cypher=node_definition),
        NodeStep(label='EnforcementPosition', csv_name='nodes_EnforcementPosition.csv', id_column='enforcementposition_id',
                 properties=['title', 'position_type', 'guidancesection_id', 'statement', 'caveat', 'criteria', 'source_document'],
                 rows=8, loadable=8, cypher=node_enforcementposition),
        NodeStep(label='ExternalReference', csv_name='nodes_ExternalReference.csv', id_column='externalreference_id',
                 properties=['citation', 'reference_type', 'source_document'],
                 rows=14, loadable=14, cypher=node_externalreference),
        NodeStep(label='GuidanceFootnote', csv_name='nodes_GuidanceFootnote.csv', id_column='guidancefootnote_id',
                 properties=['number', 'guidancesection_id', 'text', 'source_document'],
                 rows=8, loadable=8, cypher=node_guidancefootnote),
        NodeStep(label='GuidanceSection', csv_name='nodes_GuidanceSection.csv', id_column='guidancesection_id',
                 properties=['number', 'heading', 'parent_id', 'level', 'text', 'instrument_id', 'source_document'],
                 rows=14, loadable=14, cypher=node_guidancesection),
        NodeStep(label='GuidanceTerm', csv_name='nodes_GuidanceTerm.csv', id_column='guidanceterm_id',
                 properties=['term', 'definition_text', 'guidancesection_id', 'source_document'],
                 rows=5, loadable=5, cypher=node_guidanceterm),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['title', 'instrument_type', 'issuing_body', 'authority', 'source_note', 'currency_date', 'status_note', 'binding_status', 'source_document'],
                 rows=2, loadable=2, cypher=node_instrument),
        NodeStep(label='Reference', csv_name='nodes_Reference.csv', id_column='reference_id',
                 properties=['category', 'citation', 'url', 'guidancesection_id', 'source_document'],
                 rows=7, loadable=7, cypher=node_reference),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['number', 'heading', 'subpart_id', 'undesignated_text', 'amendment_note', 'source_document'],
                 rows=10, loadable=10, cypher=node_section),
        NodeStep(label='Subpart', csv_name='nodes_Subpart.csv', id_column='subpart_id',
                 properties=['designation', 'heading', 'instrument_id', 'source_document'],
                 rows=3, loadable=3, cypher=node_subpart),
        NodeStep(label='WithdrawnDocument', csv_name='nodes_WithdrawnDocument.csv', id_column='withdrawndocument_id',
                 properties=['title', 'document_type', 'withdrawal_statement', 'federal_register_citation', 'source_document'],
                 rows=6, loadable=6, cypher=node_withdrawndocument),
    ],
    rel_steps=[
        RelStep(rel_type='HAS_SUBPART', csv_name='rels_contains.csv',
                properties=['source_document'],
                rows=3, loadable=3, cypher=rel_has_subpart),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_contains.csv',
                properties=['source_document'],
                rows=10, loadable=10, cypher=rel_has_section),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_contains.csv',
                properties=['source_document'],
                rows=43, loadable=43, cypher=rel_has_clause),
        RelStep(rel_type='HAS_SUBCLAUSE', csv_name='rels_contains.csv',
                properties=['source_document'],
                rows=23, loadable=23, cypher=rel_has_subclause),
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_cross_references.csv',
                properties=['cited_as', 'quoted_context', 'source_document'],
                rows=5, loadable=5, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_defines.csv',
                properties=['citation', 'source_document'],
                rows=9, loadable=9, cypher=rel_defines),
        RelStep(rel_type='DISCRETION_APPLIES_TO', csv_name='rels_enforcement_applies_to.csv',
                properties=['stated_citation', 'source_document'],
                rows=10, loadable=10, cypher=rel_discretion_applies_to),
        RelStep(rel_type='INTERPRETS', csv_name='rels_enforcement_applies_to.csv',
                properties=['stated_citation', 'source_document'],
                rows=2, loadable=2, cypher=rel_interprets),
        RelStep(rel_type='CONTINUES_TO_ENFORCE', csv_name='rels_enforcement_applies_to.csv',
                properties=['stated_citation', 'source_document'],
                rows=8, loadable=8, cypher=rel_continues_to_enforce),
        RelStep(rel_type='REFERS_TO_EXTERNAL', csv_name='rels_external_references.csv',
                properties=['source_document'],
                rows=14, loadable=14, cypher=rel_refers_to_external),
        RelStep(rel_type='DISCUSSES', csv_name='rels_guidance_discusses.csv',
                properties=['stated_citation', 'source_document'],
                rows=21, loadable=21, cypher=rel_discusses),
        RelStep(rel_type='REFERS_TO_SECTION', csv_name='rels_guidance_discusses.csv',
                properties=['stated_citation', 'source_document'],
                rows=1, loadable=1, cypher=rel_refers_to_section),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=4, loadable=4, cypher=rel_has_section_2),
        RelStep(rel_type='HAS_SUBSECTION', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=10, loadable=10, cypher=rel_has_subsection),
        RelStep(rel_type='HAS_FOOTNOTE', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=8, loadable=8, cypher=rel_has_footnote),
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=5, loadable=5, cypher=rel_defines_term),
        RelStep(rel_type='CITES_REFERENCE', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=7, loadable=7, cypher=rel_cites_reference),
        RelStep(rel_type='ANNOUNCES', csv_name='rels_guidance_structure.csv',
                properties=['source_document'],
                rows=8, loadable=8, cypher=rel_announces),
        RelStep(rel_type='PROVIDES_GUIDANCE_ON', csv_name='rels_instrument_links.csv',
                properties=['basis', 'source_document'],
                rows=1, loadable=1, cypher=rel_provides_guidance_on),
        RelStep(rel_type='ANNOUNCES_WITHDRAWAL_OF', csv_name='rels_withdrawn_by.csv',
                properties=['source_document'],
                rows=6, loadable=6, cypher=rel_announces_withdrawal_of),
    ],
    expected_nodes_total=152,
    expected_rels_total=198,
)

if __name__ == "__main__":
    run(LOADER)
