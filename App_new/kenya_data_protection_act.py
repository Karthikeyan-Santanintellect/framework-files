"""Loader — Kenya Data Protection Act.

Source folder : New sources/Kenya Data Protection Act
Source document(s):
    Kenya Data Protection Act (No. 24 of 2019) - KB 2/Africa/Kenya Data Protection Act.pdf

Graph shape   : 8 node labels, 9 relationship types
Expected size : 586 nodes / 603 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/kenya_data_protection_act.py --dry-run   # validate, write nothing
    python App_new/kenya_data_protection_act.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'KENYA_DATA_PROTECTION_ACT'
FOLDER = 'Kenya Data Protection Act'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'KENYA_DATA_PROTECTION_ACT'})
SET f.name = 'Kenya Data Protection Act',
    f.jurisdiction = 'Kenya',
    f.folder = 'Kenya Data Protection Act',
    f.source_document = 'Kenya Data Protection Act (No. 24 of 2019) - KB 2/Africa/Kenya Data Protection Act.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'KENYA_DATA_PROTECTION_ACT'})
MATCH (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT'})
WHERE NOT (:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Act.csv  (1 rows, key act_id)
node_act = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.act_id})
  SET
      n:Act,
      n.act_id = row.act_id,
      n.title = row.title,
      n.act_number = row.act_number,
      n.jurisdiction = row.jurisdiction,
      n.date_of_assent = row.date_of_assent,
      n.date_of_commencement = row.date_of_commencement,
      n.long_title = row.long_title,
      n.publisher = row.publisher,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (447 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.section_id = row.section_id,
      n.parent_clause_id = row.parent_clause_id,
      n.label = row.label,
      n.level = row.level,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deadline.csv  (12 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.period = row.period,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (26 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition = row.definition,
      n.defined_in_section = row.defined_in_section,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (11 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.part_number = row.part_number,
      n.title = row.title,
      n.section_range = row.section_range,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (12 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.penalty_type = row.penalty_type,
      n.monetary_penalty = row.monetary_penalty,
      n.custodial_penalty = row.custodial_penalty,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Schedule.csv  (2 rows, key schedule_id)
node_schedule = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.schedule_id})
  SET
      n:Schedule,
      n.schedule_id = row.schedule_id,
      n.title = row.title,
      n.enabling_reference = row.enabling_reference,
      n.text = row.text,
      n.enabling_section_id = row.enabling_section_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (75 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.section_number = row.section_number,
      n.title = row.title,
      n.text = row.text,
      n.part_id = row.part_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_cross_references.csv -> :CROSS_REFERENCES  (16 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.reference_text = row.reference_text
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_defines.csv -> :DEFINED_IN  (26 rows)
rel_defined_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:DEFINED_IN]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_clause.csv -> :HAS_CLAUSE  (447 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
  SET r.level = row.level
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_part.csv -> :HAS_PART  (11 rows)
rel_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:HAS_PART]->(t)
  SET r.part_number = row.part_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_schedule.csv -> :HAS_SCHEDULE  (2 rows)
rel_has_schedule = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SCHEDULE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:HAS_SCHEDULE]->(t)
  SET r.title = row.title
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_section.csv -> :HAS_SECTION  (75 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.section_number = row.section_number
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_imposes_penalty.csv -> :IMPOSES_PENALTY  (12 rows)
rel_imposes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_PENALTY]->(t)
  SET r.penalty_type = row.penalty_type
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_schedule_under_section.csv -> :SCHEDULE_UNDER  (2 rows)
rel_schedule_under = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SCHEDULE_UNDER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:SCHEDULE_UNDER]->(t)
  SET r.reference = row.reference
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_sets_deadline.csv -> :SETS_DEADLINE  (12 rows)
rel_sets_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SETS_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'KENYA_DATA_PROTECTION_ACT', node_id: row.target_id})
  MERGE (s)-[r:SETS_DEADLINE]->(t)
  SET r.period = row.period
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Kenya Data Protection Act',
    folder=FOLDER,
    jurisdiction='Kenya',
    source_document='Kenya Data Protection Act (No. 24 of 2019) - KB 2/Africa/Kenya Data Protection Act.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Act', csv_name='nodes_Act.csv', id_column='act_id',
                 properties=['title', 'act_number', 'jurisdiction', 'date_of_assent', 'date_of_commencement', 'long_title', 'publisher', 'source_document'],
                 rows=1, loadable=1, cypher=node_act),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['section_id', 'parent_clause_id', 'label', 'level', 'text', 'source_document'],
                 rows=447, loadable=447, cypher=node_clause),
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['period', 'text', 'source_document'],
                 rows=12, loadable=12, cypher=node_deadline),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition', 'defined_in_section', 'source_document'],
                 rows=26, loadable=26, cypher=node_definition),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['part_number', 'title', 'section_range', 'source_document'],
                 rows=11, loadable=11, cypher=node_part),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['penalty_type', 'monetary_penalty', 'custodial_penalty', 'text', 'source_document'],
                 rows=12, loadable=12, cypher=node_penalty),
        NodeStep(label='Schedule', csv_name='nodes_Schedule.csv', id_column='schedule_id',
                 properties=['title', 'enabling_reference', 'text', 'enabling_section_id', 'source_document'],
                 rows=2, loadable=2, cypher=node_schedule),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['section_number', 'title', 'text', 'part_id', 'source_document'],
                 rows=75, loadable=75, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_cross_references.csv',
                properties=['reference_text'],
                rows=16, loadable=16, cypher=rel_cross_references),
        RelStep(rel_type='DEFINED_IN', csv_name='rels_defines.csv',
                properties=['note'],
                rows=26, loadable=26, cypher=rel_defined_in),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_has_clause.csv',
                properties=['level'],
                rows=447, loadable=447, cypher=rel_has_clause),
        RelStep(rel_type='HAS_PART', csv_name='rels_has_part.csv',
                properties=['part_number'],
                rows=11, loadable=11, cypher=rel_has_part),
        RelStep(rel_type='HAS_SCHEDULE', csv_name='rels_has_schedule.csv',
                properties=['title'],
                rows=2, loadable=2, cypher=rel_has_schedule),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_has_section.csv',
                properties=['section_number'],
                rows=75, loadable=75, cypher=rel_has_section),
        RelStep(rel_type='IMPOSES_PENALTY', csv_name='rels_imposes_penalty.csv',
                properties=['penalty_type'],
                rows=12, loadable=12, cypher=rel_imposes_penalty),
        RelStep(rel_type='SCHEDULE_UNDER', csv_name='rels_schedule_under_section.csv',
                properties=['reference'],
                rows=2, loadable=2, cypher=rel_schedule_under),
        RelStep(rel_type='SETS_DEADLINE', csv_name='rels_sets_deadline.csv',
                properties=['period'],
                rows=12, loadable=12, cypher=rel_sets_deadline),
    ],
    expected_nodes_total=586,
    expected_rels_total=603,
)

if __name__ == "__main__":
    run(LOADER)
