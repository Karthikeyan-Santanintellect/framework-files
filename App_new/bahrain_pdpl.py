"""Loader — Bahrain PDPL.

Source folder : New sources/Bahrain PDPL
Source document(s):
    KB 2/Bahrain/Bahrain Personal Data Protection Law.pdf

Graph shape   : 12 node labels, 13 relationship types
Expected size : 479 nodes / 521 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/bahrain_pdpl.py --dry-run   # validate, write nothing
    python App_new/bahrain_pdpl.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'BAHRAIN_PDPL'
FOLDER = 'Bahrain PDPL'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'BAHRAIN_PDPL'})
SET f.name = 'Bahrain PDPL',
    f.jurisdiction = 'Bahrain',
    f.folder = 'Bahrain PDPL',
    f.source_document = 'KB 2/Bahrain/Bahrain Personal Data Protection Law.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'BAHRAIN_PDPL'})
MATCH (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL'})
WHERE NOT (:NewSourceNode {framework_id: 'BAHRAIN_PDPL'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Article.csv  (60 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.section_id = row.section_id,
      n.part_id = row.part_id,
      n.article_number = row.article_number,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AuthorityBody.csv  (5 rows, key authoritybody_id)
node_authoritybody = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.authoritybody_id})
  SET
      n:AuthorityBody,
      n.authoritybody_id = row.authoritybody_id,
      n.name = row.name,
      n.body_type = row.body_type,
      n.established_by_article = row.established_by_article,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (310 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.parent_clause_id = row.parent_clause_id,
      n.label = row.label,
      n.level = row.level,
      n.citation = row.citation,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DataSubjectRight.csv  (9 rows, key datasubjectright_id)
node_datasubjectright = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.datasubjectright_id})
  SET
      n:DataSubjectRight,
      n.datasubjectright_id = row.datasubjectright_id,
      n.name = row.name,
      n.article_id = row.article_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Deadline.csv  (29 rows, key deadline_id)
node_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.deadline_id})
  SET
      n:Deadline,
      n.deadline_id = row.deadline_id,
      n.clause_id = row.clause_id,
      n.period_text = row.period_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (21 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition = row.definition,
      n.defined_in_article = row.defined_in_article,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.title = row.title,
      n.short_title = row.short_title,
      n.jurisdiction = row.jurisdiction,
      n.promulgated_by = row.promulgated_by,
      n.promulgation_place = row.promulgation_place,
      n.promulgation_date_hijri = row.promulgation_date_hijri,
      n.promulgation_date_gregorian = row.promulgation_date_gregorian,
      n.approving_bodies = row.approving_bodies,
      n.language_note = row.language_note,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (3 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.label = row.label,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Penalty.csv  (5 rows, key penalty_id)
node_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.penalty_id})
  SET
      n:Penalty,
      n.penalty_id = row.penalty_id,
      n.clause_id = row.clause_id,
      n.penalty_type = row.penalty_type,
      n.imprisonment = row.imprisonment,
      n.fine_min = row.fine_min,
      n.fine_max = row.fine_max,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_PreambleCitation.csv  (21 rows, key preamblecitation_id)
node_preamblecitation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.preamblecitation_id})
  SET
      n:PreambleCitation,
      n.preamblecitation_id = row.preamblecitation_id,
      n.order = row.order,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_PromulgationArticle.csv  (4 rows, key promulgationarticle_id)
node_promulgationarticle = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.promulgationarticle_id})
  SET
      n:PromulgationArticle,
      n.promulgationarticle_id = row.promulgationarticle_id,
      n.label = row.label,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (11 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.part_id = row.part_id,
      n.label = row.label,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_DEFINES_TERM.csv -> :DEFINES_TERM  (21 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_ESTABLISHES_BODY.csv -> :ESTABLISHES_BODY  (5 rows)
rel_establishes_body = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ESTABLISHES_BODY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:ESTABLISHES_BODY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_GRANTS_RIGHT.csv -> :GRANTS_RIGHT  (9 rows)
rel_grants_right = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'GRANTS_RIGHT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:GRANTS_RIGHT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTICLE.csv -> :HAS_ARTICLE  (60 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (175 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PART.csv -> :HAS_PART  (3 rows)
rel_has_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PART'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_PART]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PREAMBLE_CITATION.csv -> :HAS_PREAMBLE_CITATION  (21 rows)
rel_has_preamble_citation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PREAMBLE_CITATION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_PREAMBLE_CITATION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PROMULGATION_ARTICLE.csv -> :HAS_PROMULGATION_ARTICLE  (4 rows)
rel_has_promulgation_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PROMULGATION_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_PROMULGATION_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (11 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBCLAUSE.csv -> :HAS_SUBCLAUSE  (135 rows)
rel_has_subclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBCLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBCLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_PRESCRIBES_PENALTY.csv -> :PRESCRIBES_PENALTY  (5 rows)
rel_prescribes_penalty = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PRESCRIBES_PENALTY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:PRESCRIBES_PENALTY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_REFERENCES_ARTICLE.csv -> :REFERENCES_ARTICLE  (43 rows)
rel_references_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_ARTICLE]->(t)
  SET r.citation = row.citation
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_SETS_DEADLINE.csv -> :SETS_DEADLINE  (29 rows)
rel_sets_deadline = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'SETS_DEADLINE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'BAHRAIN_PDPL', node_id: row.target_id})
  MERGE (s)-[r:SETS_DEADLINE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Bahrain PDPL',
    folder=FOLDER,
    jurisdiction='Bahrain',
    source_document='KB 2/Bahrain/Bahrain Personal Data Protection Law.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['section_id', 'part_id', 'article_number', 'heading', 'text', 'source_document'],
                 rows=60, loadable=60, cypher=node_article),
        NodeStep(label='AuthorityBody', csv_name='nodes_AuthorityBody.csv', id_column='authoritybody_id',
                 properties=['name', 'body_type', 'established_by_article', 'text', 'source_document'],
                 rows=5, loadable=5, cypher=node_authoritybody),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['article_id', 'parent_clause_id', 'label', 'level', 'citation', 'text', 'source_document'],
                 rows=310, loadable=310, cypher=node_clause),
        NodeStep(label='DataSubjectRight', csv_name='nodes_DataSubjectRight.csv', id_column='datasubjectright_id',
                 properties=['name', 'article_id', 'source_document'],
                 rows=9, loadable=9, cypher=node_datasubjectright),
        NodeStep(label='Deadline', csv_name='nodes_Deadline.csv', id_column='deadline_id',
                 properties=['clause_id', 'period_text', 'source_document'],
                 rows=29, loadable=29, cypher=node_deadline),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition', 'defined_in_article', 'source_document'],
                 rows=21, loadable=21, cypher=node_definition),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['title', 'short_title', 'jurisdiction', 'promulgated_by', 'promulgation_place', 'promulgation_date_hijri', 'promulgation_date_gregorian', 'approving_bodies', 'language_note', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['label', 'title', 'source_document'],
                 rows=3, loadable=3, cypher=node_part),
        NodeStep(label='Penalty', csv_name='nodes_Penalty.csv', id_column='penalty_id',
                 properties=['clause_id', 'penalty_type', 'imprisonment', 'fine_min', 'fine_max', 'text', 'source_document'],
                 rows=5, loadable=5, cypher=node_penalty),
        NodeStep(label='PreambleCitation', csv_name='nodes_PreambleCitation.csv', id_column='preamblecitation_id',
                 properties=['order', 'text', 'source_document'],
                 rows=21, loadable=21, cypher=node_preamblecitation),
        NodeStep(label='PromulgationArticle', csv_name='nodes_PromulgationArticle.csv', id_column='promulgationarticle_id',
                 properties=['label', 'text', 'source_document'],
                 rows=4, loadable=4, cypher=node_promulgationarticle),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['part_id', 'label', 'title', 'source_document'],
                 rows=11, loadable=11, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_DEFINES_TERM.csv',
                properties=[],
                rows=21, loadable=21, cypher=rel_defines_term),
        RelStep(rel_type='ESTABLISHES_BODY', csv_name='rels_ESTABLISHES_BODY.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_establishes_body),
        RelStep(rel_type='GRANTS_RIGHT', csv_name='rels_GRANTS_RIGHT.csv',
                properties=[],
                rows=9, loadable=9, cypher=rel_grants_right),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_HAS_ARTICLE.csv',
                properties=[],
                rows=60, loadable=60, cypher=rel_has_article),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=[],
                rows=175, loadable=175, cypher=rel_has_clause),
        RelStep(rel_type='HAS_PART', csv_name='rels_HAS_PART.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_has_part),
        RelStep(rel_type='HAS_PREAMBLE_CITATION', csv_name='rels_HAS_PREAMBLE_CITATION.csv',
                properties=[],
                rows=21, loadable=21, cypher=rel_has_preamble_citation),
        RelStep(rel_type='HAS_PROMULGATION_ARTICLE', csv_name='rels_HAS_PROMULGATION_ARTICLE.csv',
                properties=[],
                rows=4, loadable=4, cypher=rel_has_promulgation_article),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=11, loadable=11, cypher=rel_has_section),
        RelStep(rel_type='HAS_SUBCLAUSE', csv_name='rels_HAS_SUBCLAUSE.csv',
                properties=[],
                rows=135, loadable=135, cypher=rel_has_subclause),
        RelStep(rel_type='PRESCRIBES_PENALTY', csv_name='rels_PRESCRIBES_PENALTY.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_prescribes_penalty),
        RelStep(rel_type='REFERENCES_ARTICLE', csv_name='rels_REFERENCES_ARTICLE.csv',
                properties=['citation'],
                rows=43, loadable=43, cypher=rel_references_article),
        RelStep(rel_type='SETS_DEADLINE', csv_name='rels_SETS_DEADLINE.csv',
                properties=[],
                rows=29, loadable=29, cypher=rel_sets_deadline),
    ],
    expected_nodes_total=479,
    expected_rels_total=521,
)

if __name__ == "__main__":
    run(LOADER)
