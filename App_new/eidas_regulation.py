"""Loader — eIDAS Regulation.

Source folder : New sources/eIDAS Regulation
Source document(s):
    KB 2/Europe/eIDAS Regulation (Electronic Identification and Trust Services).pdf

Graph shape   : 10 node labels, 16 relationship types
Expected size : 598 nodes / 752 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/eidas_regulation.py --dry-run   # validate, write nothing
    python App_new/eidas_regulation.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'EIDAS_REGULATION'
FOLDER = 'eIDAS Regulation'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'EIDAS_REGULATION'})
SET f.name = 'eIDAS Regulation',
    f.jurisdiction = 'European Union',
    f.folder = 'eIDAS Regulation',
    f.source_document = 'KB 2/Europe/eIDAS Regulation (Electronic Identification and Trust Services).pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'EIDAS_REGULATION'})
MATCH (n:NewSourceNode {framework_id: 'EIDAS_REGULATION'})
WHERE NOT (:NewSourceNode {framework_id: 'EIDAS_REGULATION'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Annex.csv  (4 rows, key annex_id)
node_annex = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.annex_id})
  SET
      n:Annex,
      n.annex_id = row.annex_id,
      n.annex_number = row.annex_number,
      n.title = row.title,
      n.full_text = row.full_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AnnexRequirement.csv  (50 rows, key annexrequirement_id)
node_annexrequirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.annexrequirement_id})
  SET
      n:AnnexRequirement,
      n.annexrequirement_id = row.annexrequirement_id,
      n.annex_id = row.annex_id,
      n.requirement_kind = row.requirement_kind,
      n.label = row.label,
      n.parent_requirement_id = row.parent_requirement_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Article.csv  (52 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.article_number = row.article_number,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.section_id = row.section_id,
      n.chapeau = row.chapeau,
      n.full_text = row.full_text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_AssuranceLevel.csv  (3 rows, key assurance_level_id)
node_assurancelevel = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.assurance_level_id})
  SET
      n:AssuranceLevel,
      n.assurance_level_id = row.assurance_level_id,
      n.level_name = row.level_name,
      n.criteria_text = row.criteria_text,
      n.defined_in_article_id = row.defined_in_article_id,
      n.provision_reference = row.provision_reference,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (6 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.chapter_number = row.chapter_number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (41 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.definition_number = row.definition_number,
      n.term = row.term,
      n.definition_text = row.definition_text,
      n.full_text = row.full_text,
      n.defined_in_article_id = row.defined_in_article_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Provision.csv  (356 rows, key provision_id)
node_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.provision_id})
  SET
      n:Provision,
      n.provision_id = row.provision_id,
      n.article_id = row.article_id,
      n.article_number = row.article_number,
      n.provision_kind = row.provision_kind,
      n.label = row.label,
      n.parent_provision_id = row.parent_provision_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Recital.csv  (77 rows, key recital_id)
node_recital = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.recital_id})
  SET
      n:Recital,
      n.recital_id = row.recital_id,
      n.recital_number = row.recital_number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Regulation.csv  (1 rows, key regulation_id)
node_regulation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.regulation_id})
  SET
      n:Regulation,
      n.regulation_id = row.regulation_id,
      n.celex_number = row.celex_number,
      n.short_name = row.short_name,
      n.official_title = row.official_title,
      n.adopted_date = row.adopted_date,
      n.oj_reference = row.oj_reference,
      n.entry_into_force = row.entry_into_force,
      n.repeals = row.repeals,
      n.legal_basis = row.legal_basis,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (8 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.section_number = row.section_number,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_annex_has_requirement.csv -> :HAS_REQUIREMENT  (7 rows)
rel_has_requirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_REQUIREMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_REQUIREMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_article_defines_assurance_level.csv -> :DEFINES_ASSURANCE_LEVEL  (3 rows)
rel_defines_assurance_level = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_ASSURANCE_LEVEL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_ASSURANCE_LEVEL]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_article_defines_term.csv -> :DEFINES_TERM  (41 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_article_has_provision.csv -> :HAS_PROVISION  (160 rows)
rel_has_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PROVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_PROVISION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_chapter_has_article.csv -> :HAS_ARTICLE  (52 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_chapter_has_section.csv -> :HAS_SECTION  (8 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_definition_references.csv -> :REFERENCES_ARTICLE  (6 rows)
rel_references_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_definition_references.csv -> :REFERENCES_ANNEX  (5 rows)
rel_references_annex = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_ANNEX'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_ANNEX]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_provision_has_subprovision.csv -> :HAS_SUBPROVISION  (196 rows)
rel_has_subprovision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBPROVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBPROVISION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_provision_references.csv -> :REFERENCES_ARTICLE  (94 rows)
rel_references_article_2 = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_provision_references.csv -> :REFERENCES_ANNEX  (17 rows)
rel_references_annex_2 = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'REFERENCES_ANNEX'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:REFERENCES_ANNEX]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_regulation_has_annex.csv -> :HAS_ANNEX  (4 rows)
rel_has_annex = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ANNEX'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_ANNEX]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_regulation_has_chapter.csv -> :HAS_CHAPTER  (6 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_regulation_has_recital.csv -> :HAS_RECITAL  (77 rows)
rel_has_recital = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_RECITAL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_RECITAL]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_requirement_has_subrequirement.csv -> :HAS_SUBREQUIREMENT  (43 rows)
rel_has_subrequirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBREQUIREMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBREQUIREMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_section_has_article.csv -> :HAS_ARTICLE  (33 rows)
rel_has_article_2 = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'EIDAS_REGULATION', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='eIDAS Regulation',
    folder=FOLDER,
    jurisdiction='European Union',
    source_document='KB 2/Europe/eIDAS Regulation (Electronic Identification and Trust Services).pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Annex', csv_name='nodes_Annex.csv', id_column='annex_id',
                 properties=['annex_number', 'title', 'full_text', 'source_document'],
                 rows=4, loadable=4, cypher=node_annex),
        NodeStep(label='AnnexRequirement', csv_name='nodes_AnnexRequirement.csv', id_column='annexrequirement_id',
                 properties=['annex_id', 'requirement_kind', 'label', 'parent_requirement_id', 'text', 'source_document'],
                 rows=50, loadable=50, cypher=node_annexrequirement),
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['article_number', 'title', 'chapter_id', 'section_id', 'chapeau', 'full_text', 'source_document'],
                 rows=52, loadable=52, cypher=node_article),
        NodeStep(label='AssuranceLevel', csv_name='nodes_AssuranceLevel.csv', id_column='assurance_level_id',
                 properties=['level_name', 'criteria_text', 'defined_in_article_id', 'provision_reference', 'source_document'],
                 rows=3, loadable=3, cypher=node_assurancelevel),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['chapter_number', 'title', 'source_document'],
                 rows=6, loadable=6, cypher=node_chapter),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['definition_number', 'term', 'definition_text', 'full_text', 'defined_in_article_id', 'source_document'],
                 rows=41, loadable=41, cypher=node_definition),
        NodeStep(label='Provision', csv_name='nodes_Provision.csv', id_column='provision_id',
                 properties=['article_id', 'article_number', 'provision_kind', 'label', 'parent_provision_id', 'text', 'source_document'],
                 rows=356, loadable=356, cypher=node_provision),
        NodeStep(label='Recital', csv_name='nodes_Recital.csv', id_column='recital_id',
                 properties=['recital_number', 'text', 'source_document'],
                 rows=77, loadable=77, cypher=node_recital),
        NodeStep(label='Regulation', csv_name='nodes_Regulation.csv', id_column='regulation_id',
                 properties=['celex_number', 'short_name', 'official_title', 'adopted_date', 'oj_reference', 'entry_into_force', 'repeals', 'legal_basis', 'source_document'],
                 rows=1, loadable=1, cypher=node_regulation),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['section_number', 'title', 'chapter_id', 'source_document'],
                 rows=8, loadable=8, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='HAS_REQUIREMENT', csv_name='rels_annex_has_requirement.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_has_requirement),
        RelStep(rel_type='DEFINES_ASSURANCE_LEVEL', csv_name='rels_article_defines_assurance_level.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_defines_assurance_level),
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_article_defines_term.csv',
                properties=[],
                rows=41, loadable=41, cypher=rel_defines_term),
        RelStep(rel_type='HAS_PROVISION', csv_name='rels_article_has_provision.csv',
                properties=[],
                rows=160, loadable=160, cypher=rel_has_provision),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_chapter_has_article.csv',
                properties=[],
                rows=52, loadable=52, cypher=rel_has_article),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_chapter_has_section.csv',
                properties=[],
                rows=8, loadable=8, cypher=rel_has_section),
        RelStep(rel_type='REFERENCES_ARTICLE', csv_name='rels_definition_references.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_references_article),
        RelStep(rel_type='REFERENCES_ANNEX', csv_name='rels_definition_references.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_references_annex),
        RelStep(rel_type='HAS_SUBPROVISION', csv_name='rels_provision_has_subprovision.csv',
                properties=[],
                rows=196, loadable=196, cypher=rel_has_subprovision),
        RelStep(rel_type='REFERENCES_ARTICLE', csv_name='rels_provision_references.csv',
                properties=[],
                rows=94, loadable=94, cypher=rel_references_article_2),
        RelStep(rel_type='REFERENCES_ANNEX', csv_name='rels_provision_references.csv',
                properties=[],
                rows=17, loadable=17, cypher=rel_references_annex_2),
        RelStep(rel_type='HAS_ANNEX', csv_name='rels_regulation_has_annex.csv',
                properties=[],
                rows=4, loadable=4, cypher=rel_has_annex),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_regulation_has_chapter.csv',
                properties=[],
                rows=6, loadable=6, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_RECITAL', csv_name='rels_regulation_has_recital.csv',
                properties=[],
                rows=77, loadable=77, cypher=rel_has_recital),
        RelStep(rel_type='HAS_SUBREQUIREMENT', csv_name='rels_requirement_has_subrequirement.csv',
                properties=[],
                rows=43, loadable=43, cypher=rel_has_subrequirement),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_section_has_article.csv',
                properties=[],
                rows=33, loadable=33, cypher=rel_has_article_2),
    ],
    expected_nodes_total=598,
    expected_rels_total=752,
)

if __name__ == "__main__":
    run(LOADER)
