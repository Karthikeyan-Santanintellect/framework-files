"""Loader — Argentina PDPA.

Source folder : New sources/Argentina PDPA
Source document(s):
    KB 2/South America/Argentina Personal Data Protection Act 2000.pdf

Graph shape   : 10 node labels, 11 relationship types
Expected size : 265 nodes / 282 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/argentina_pdpa.py --dry-run   # validate, write nothing
    python App_new/argentina_pdpa.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'ARGENTINA_PDPA'
FOLDER = 'Argentina PDPA'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'ARGENTINA_PDPA'})
SET f.name = 'Argentina PDPA',
    f.jurisdiction = 'Argentina',
    f.folder = 'Argentina PDPA',
    f.source_document = 'KB 2/South America/Argentina Personal Data Protection Act 2000.pdf',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'ARGENTINA_PDPA'})
MATCH (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA'})
WHERE NOT (:NewSourceNode {framework_id: 'ARGENTINA_PDPA'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Article.csv  (48 rows, key article_id)
node_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.article_id})
  SET
      n:Article,
      n.article_id = row.article_id,
      n.number = row.number,
      n.heading = row.heading,
      n.title = row.title,
      n.chapter_id = row.chapter_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Chapter.csv  (7 rows, key chapter_id)
node_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.chapter_id})
  SET
      n:Chapter,
      n.chapter_id = row.chapter_id,
      n.number = row.number,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Clause.csv  (118 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.label = row.label,
      n.text = row.text,
      n.deadline_or_term = row.deadline_or_term,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Decree.csv  (1 rows, key decree_id)
node_decree = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.decree_id})
  SET
      n:Decree,
      n.decree_id = row.decree_id,
      n.title = row.title,
      n.context = row.context,
      n.issuing_authority = row.issuing_authority,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DecreeArticle.csv  (5 rows, key decree_article_id)
node_decreearticle = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.decree_article_id})
  SET
      n:DecreeArticle,
      n.decree_article_id = row.decree_article_id,
      n.decree_id = row.decree_id,
      n.number = row.number,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (9 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition = row.definition,
      n.defined_in_article = row.defined_in_article,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.short_title = row.short_title,
      n.law_number = row.law_number,
      n.year = row.year,
      n.jurisdiction = row.jurisdiction,
      n.language_of_text = row.language_of_text,
      n.subject_matter = row.subject_matter,
      n.enacting_formula = row.enacting_formula,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Recital.csv  (15 rows, key recital_id)
node_recital = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.recital_id})
  SET
      n:Recital,
      n.recital_id = row.recital_id,
      n.decree_id = row.decree_id,
      n.sequence = row.sequence,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Sanction.csv  (7 rows, key sanction_id)
node_sanction = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.sanction_id})
  SET
      n:Sanction,
      n.sanction_id = row.sanction_id,
      n.sanction_type = row.sanction_type,
      n.article_id = row.article_id,
      n.penalty_text = row.penalty_text,
      n.minimum = row.minimum,
      n.maximum = row.maximum,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SubClause.csv  (54 rows, key subclause_id)
node_subclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.subclause_id})
  SET
      n:SubClause,
      n.subclause_id = row.subclause_id,
      n.clause_id = row.clause_id,
      n.article_id = row.article_id,
      n.label = row.label,
      n.text = row.text,
      n.deadline_or_term = row.deadline_or_term,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (16 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (9 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_ENACTED_BY_DECREE.csv -> :ENACTED_BY_DECREE  (1 rows)
rel_enacted_by_decree = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'ENACTED_BY_DECREE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:ENACTED_BY_DECREE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_ARTICLE.csv -> :HAS_ARTICLE  (48 rows)
rel_has_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_ARTICLE]->(t)
  SET r.note = row.note
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CHAPTER.csv -> :HAS_CHAPTER  (7 rows)
rel_has_chapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CHAPTER'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CHAPTER]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (118 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DECREE_ARTICLE.csv -> :HAS_DECREE_ARTICLE  (5 rows)
rel_has_decree_article = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DECREE_ARTICLE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_DECREE_ARTICLE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_RECITAL.csv -> :HAS_RECITAL  (15 rows)
rel_has_recital = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_RECITAL'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_RECITAL]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBCLAUSE.csv -> :HAS_SUBCLAUSE  (54 rows)
rel_has_subclause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBCLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBCLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_IMPOSES_SANCTION.csv -> :IMPOSES_SANCTION  (7 rows)
rel_imposes_sanction = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'IMPOSES_SANCTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:IMPOSES_SANCTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_OBSERVES.csv -> :OBSERVES  (2 rows)
rel_observes = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'OBSERVES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'ARGENTINA_PDPA', node_id: row.target_id})
  MERGE (s)-[r:OBSERVES]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Argentina PDPA',
    folder=FOLDER,
    jurisdiction='Argentina',
    source_document='KB 2/South America/Argentina Personal Data Protection Act 2000.pdf',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Article', csv_name='nodes_Article.csv', id_column='article_id',
                 properties=['number', 'heading', 'title', 'chapter_id', 'source_document'],
                 rows=48, loadable=48, cypher=node_article),
        NodeStep(label='Chapter', csv_name='nodes_Chapter.csv', id_column='chapter_id',
                 properties=['number', 'title', 'source_document'],
                 rows=7, loadable=7, cypher=node_chapter),
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['article_id', 'label', 'text', 'deadline_or_term', 'source_document'],
                 rows=118, loadable=118, cypher=node_clause),
        NodeStep(label='Decree', csv_name='nodes_Decree.csv', id_column='decree_id',
                 properties=['title', 'context', 'issuing_authority', 'source_document'],
                 rows=1, loadable=1, cypher=node_decree),
        NodeStep(label='DecreeArticle', csv_name='nodes_DecreeArticle.csv', id_column='decree_article_id',
                 properties=['decree_id', 'number', 'text', 'source_document'],
                 rows=5, loadable=5, cypher=node_decreearticle),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition', 'defined_in_article', 'source_document'],
                 rows=9, loadable=9, cypher=node_definition),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['short_title', 'law_number', 'year', 'jurisdiction', 'language_of_text', 'subject_matter', 'enacting_formula', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='Recital', csv_name='nodes_Recital.csv', id_column='recital_id',
                 properties=['decree_id', 'sequence', 'text', 'source_document'],
                 rows=15, loadable=15, cypher=node_recital),
        NodeStep(label='Sanction', csv_name='nodes_Sanction.csv', id_column='sanction_id',
                 properties=['sanction_type', 'article_id', 'penalty_text', 'minimum', 'maximum', 'source_document'],
                 rows=7, loadable=7, cypher=node_sanction),
        NodeStep(label='SubClause', csv_name='nodes_SubClause.csv', id_column='subclause_id',
                 properties=['clause_id', 'article_id', 'label', 'text', 'deadline_or_term', 'source_document'],
                 rows=54, loadable=54, cypher=node_subclause),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=[],
                rows=16, loadable=16, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=[],
                rows=9, loadable=9, cypher=rel_defines),
        RelStep(rel_type='ENACTED_BY_DECREE', csv_name='rels_ENACTED_BY_DECREE.csv',
                properties=[],
                rows=1, loadable=1, cypher=rel_enacted_by_decree),
        RelStep(rel_type='HAS_ARTICLE', csv_name='rels_HAS_ARTICLE.csv',
                properties=['note'],
                rows=48, loadable=48, cypher=rel_has_article),
        RelStep(rel_type='HAS_CHAPTER', csv_name='rels_HAS_CHAPTER.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_has_chapter),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=[],
                rows=118, loadable=118, cypher=rel_has_clause),
        RelStep(rel_type='HAS_DECREE_ARTICLE', csv_name='rels_HAS_DECREE_ARTICLE.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_has_decree_article),
        RelStep(rel_type='HAS_RECITAL', csv_name='rels_HAS_RECITAL.csv',
                properties=[],
                rows=15, loadable=15, cypher=rel_has_recital),
        RelStep(rel_type='HAS_SUBCLAUSE', csv_name='rels_HAS_SUBCLAUSE.csv',
                properties=[],
                rows=54, loadable=54, cypher=rel_has_subclause),
        RelStep(rel_type='IMPOSES_SANCTION', csv_name='rels_IMPOSES_SANCTION.csv',
                properties=[],
                rows=7, loadable=7, cypher=rel_imposes_sanction),
        RelStep(rel_type='OBSERVES', csv_name='rels_OBSERVES.csv',
                properties=[],
                rows=2, loadable=2, cypher=rel_observes),
    ],
    expected_nodes_total=265,
    expected_rels_total=282,
)

if __name__ == "__main__":
    run(LOADER)
