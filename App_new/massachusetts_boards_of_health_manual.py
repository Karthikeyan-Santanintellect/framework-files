"""Loader — Massachusetts Boards of Health Manual.

Source folder : New sources/Massachusetts Boards of Health Manual
Source document(s):
    Massachusetts Association of Health Boards, Manual of Laws and Regulations Relating to Boards of Health, June 2016 (KB 2/USA/MAHB-updated-Manual-of-Laws-and-Regulations-6.17.16-1.pdf)

Graph shape   : 8 node labels, 8 relationship types
Expected size : 372 nodes / 394 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/massachusetts_boards_of_health_manual.py --dry-run   # validate, write nothing
    python App_new/massachusetts_boards_of_health_manual.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL'
FOLDER = 'Massachusetts Boards of Health Manual'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL'})
SET f.name = 'Massachusetts Boards of Health Manual',
    f.jurisdiction = 'United States',
    f.folder = 'Massachusetts Boards of Health Manual',
    f.source_document = 'Massachusetts Association of Health Boards, Manual of Laws and Regulations Relating to Boards of Health, June 2016 (KB 2/USA/MAHB-updated-Manual-of-Laws-and-Regulations-6.17.16-1.pdf)',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL'})
MATCH (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL'})
WHERE NOT (:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_BoardPower.csv  (77 rows, key boardpower_id)
node_boardpower = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.boardpower_id})
  SET
      n:BoardPower,
      n.boardpower_id = row.boardpower_id,
      n.power_type = row.power_type,
      n.modality = row.modality,
      n.part_id = row.part_id,
      n.section_id = row.section_id,
      n.provision_id = row.provision_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Citation.csv  (140 rows, key citation_id)
node_citation = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.citation_id})
  SET
      n:Citation,
      n.citation_id = row.citation_id,
      n.citation_text = row.citation_text,
      n.citation_type = row.citation_type,
      n.code = row.code,
      n.chapter_or_title = row.chapter_or_title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DefinedTerm.csv  (6 rows, key definedterm_id)
node_definedterm = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.definedterm_id})
  SET
      n:DefinedTerm,
      n.definedterm_id = row.definedterm_id,
      n.term = row.term,
      n.definition = row.definition,
      n.defined_in = row.defined_in,
      n.provision_id = row.provision_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Part.csv  (8 rows, key part_id)
node_part = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.part_id})
  SET
      n:Part,
      n.part_id = row.part_id,
      n.part_number = row.part_number,
      n.part_order = row.part_order,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Provision.csv  (62 rows, key provision_id)
node_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.provision_id})
  SET
      n:Provision,
      n.provision_id = row.provision_id,
      n.part_id = row.part_id,
      n.section_id = row.section_id,
      n.block_type = row.block_type,
      n.page = row.page,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_RecordsProvision.csv  (18 rows, key recordsprovision_id)
node_recordsprovision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.recordsprovision_id})
  SET
      n:RecordsProvision,
      n.recordsprovision_id = row.recordsprovision_id,
      n.category = row.category,
      n.concerns_identifiable_persons = row.concerns_identifiable_persons,
      n.part_id = row.part_id,
      n.section_id = row.section_id,
      n.provision_id = row.provision_id,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_SanitaryCodeChapter.csv  (10 rows, key sanitarycodechapter_id)
node_sanitarycodechapter = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.sanitarycodechapter_id})
  SET
      n:SanitaryCodeChapter,
      n.sanitarycodechapter_id = row.sanitarycodechapter_id,
      n.chapter_number = row.chapter_number,
      n.subject = row.subject,
      n.cmr_citation = row.cmr_citation,
      n.regulation_title = row.regulation_title,
      n.section_id = row.section_id,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (51 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.part_id = row.part_id,
      n.letter = row.letter,
      n.title = row.title,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_boardpower_stated_in.csv -> :STATED_IN  (77 rows)
rel_stated_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'STATED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:STATED_IN]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_definedterm_defined_in.csv -> :DEFINED_IN  (6 rows)
rel_defined_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:DEFINED_IN]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_has_provision.csv -> :HAS_PROVISION  (62 rows)
rel_has_provision = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PROVISION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:HAS_PROVISION]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_part_has_section.csv -> :HAS_SECTION  (51 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_provision_cites_citation.csv -> :CITES  (160 rows)
rel_cites = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CITES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:CITES]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_recordsprovision_derived_from.csv -> :DERIVED_FROM  (18 rows)
rel_derived_from = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DERIVED_FROM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:DERIVED_FROM]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_sanitarycodechapter_links.csv -> :PROMULGATED_AS  (10 rows)
rel_promulgated_as = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'PROMULGATED_AS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:PROMULGATED_AS]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_sanitarycodechapter_links.csv -> :DESCRIBED_IN  (10 rows)
rel_described_in = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DESCRIBED_IN'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'MASSACHUSETTS_BOARDS_OF_HEALTH_MANUAL', node_id: row.target_id})
  MERGE (s)-[r:DESCRIBED_IN]->(t)
  SET r.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='Massachusetts Boards of Health Manual',
    folder=FOLDER,
    jurisdiction='United States',
    source_document='Massachusetts Association of Health Boards, Manual of Laws and Regulations Relating to Boards of Health, June 2016 (KB 2/USA/MAHB-updated-Manual-of-Laws-and-Regulations-6.17.16-1.pdf)',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='BoardPower', csv_name='nodes_BoardPower.csv', id_column='boardpower_id',
                 properties=['power_type', 'modality', 'part_id', 'section_id', 'provision_id', 'text', 'source_document'],
                 rows=77, loadable=77, cypher=node_boardpower),
        NodeStep(label='Citation', csv_name='nodes_Citation.csv', id_column='citation_id',
                 properties=['citation_text', 'citation_type', 'code', 'chapter_or_title', 'source_document'],
                 rows=140, loadable=140, cypher=node_citation),
        NodeStep(label='DefinedTerm', csv_name='nodes_DefinedTerm.csv', id_column='definedterm_id',
                 properties=['term', 'definition', 'defined_in', 'provision_id', 'source_document'],
                 rows=6, loadable=6, cypher=node_definedterm),
        NodeStep(label='Part', csv_name='nodes_Part.csv', id_column='part_id',
                 properties=['part_number', 'part_order', 'title', 'source_document'],
                 rows=8, loadable=8, cypher=node_part),
        NodeStep(label='Provision', csv_name='nodes_Provision.csv', id_column='provision_id',
                 properties=['part_id', 'section_id', 'block_type', 'page', 'text', 'source_document'],
                 rows=62, loadable=62, cypher=node_provision),
        NodeStep(label='RecordsProvision', csv_name='nodes_RecordsProvision.csv', id_column='recordsprovision_id',
                 properties=['category', 'concerns_identifiable_persons', 'part_id', 'section_id', 'provision_id', 'text', 'source_document'],
                 rows=18, loadable=18, cypher=node_recordsprovision),
        NodeStep(label='SanitaryCodeChapter', csv_name='nodes_SanitaryCodeChapter.csv', id_column='sanitarycodechapter_id',
                 properties=['chapter_number', 'subject', 'cmr_citation', 'regulation_title', 'section_id', 'source_document'],
                 rows=10, loadable=10, cypher=node_sanitarycodechapter),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['part_id', 'letter', 'title', 'source_document'],
                 rows=51, loadable=51, cypher=node_section),
    ],
    rel_steps=[
        RelStep(rel_type='STATED_IN', csv_name='rels_boardpower_stated_in.csv',
                properties=['source_document'],
                rows=77, loadable=77, cypher=rel_stated_in),
        RelStep(rel_type='DEFINED_IN', csv_name='rels_definedterm_defined_in.csv',
                properties=['source_document'],
                rows=6, loadable=6, cypher=rel_defined_in),
        RelStep(rel_type='HAS_PROVISION', csv_name='rels_has_provision.csv',
                properties=['source_document'],
                rows=62, loadable=62, cypher=rel_has_provision),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_part_has_section.csv',
                properties=['source_document'],
                rows=51, loadable=51, cypher=rel_has_section),
        RelStep(rel_type='CITES', csv_name='rels_provision_cites_citation.csv',
                properties=['source_document'],
                rows=160, loadable=160, cypher=rel_cites),
        RelStep(rel_type='DERIVED_FROM', csv_name='rels_recordsprovision_derived_from.csv',
                properties=['source_document'],
                rows=18, loadable=18, cypher=rel_derived_from),
        RelStep(rel_type='PROMULGATED_AS', csv_name='rels_sanitarycodechapter_links.csv',
                properties=['source_document'],
                rows=10, loadable=10, cypher=rel_promulgated_as),
        RelStep(rel_type='DESCRIBED_IN', csv_name='rels_sanitarycodechapter_links.csv',
                properties=['source_document'],
                rows=10, loadable=10, cypher=rel_described_in),
    ],
    expected_nodes_total=372,
    expected_rels_total=394,
)

if __name__ == "__main__":
    run(LOADER)
