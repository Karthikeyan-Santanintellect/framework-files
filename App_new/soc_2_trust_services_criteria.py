"""Loader — SOC 2 Trust Services Criteria.

Source folder : New sources/SOC 2 Trust Services Criteria
Source document(s):
    AICPA TSP Section 100 — 2017 Trust Services Criteria for Security, Availability, Processing Integrity, Confidentiality, and Privacy (includes March 2020 updates) [KB 2/USA/SOC 2.pdf]

Graph shape   : 8 node labels, 8 relationship types
Expected size : 474 nodes / 666 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/soc_2_trust_services_criteria.py --dry-run   # validate, write nothing
    python App_new/soc_2_trust_services_criteria.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'SOC_2_TRUST_SERVICES_CRITERIA'
FOLDER = 'SOC 2 Trust Services Criteria'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA'})
SET f.name = 'SOC 2 Trust Services Criteria',
    f.jurisdiction = 'United States',
    f.folder = 'SOC 2 Trust Services Criteria',
    f.source_document = 'AICPA TSP Section 100 — 2017 Trust Services Criteria for Security, Availability, Processing Integrity, Confidentiality, and Privacy (includes March 2020 updates) [KB 2/USA/SOC 2.pdf]',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA'})
MATCH (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA'})
WHERE NOT (:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_CriteriaGroup.csv  (12 rows, key group_id)
node_criteriagroup = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.group_id})
  SET
      n:CriteriaGroup,
      n.group_id = row.group_id,
      n.ref = row.ref,
      n.name = row.name,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Criterion.csv  (61 rows, key criterion_id)
node_criterion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.criterion_id})
  SET
      n:Criterion,
      n.criterion_id = row.criterion_id,
      n.ref = row.ref,
      n.series = row.series,
      n.coso_principle = row.coso_principle,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (63 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.term = row.term,
      n.definition = row.definition,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Document.csv  (1 rows, key document_id)
node_document = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.document_id})
  SET
      n:Document,
      n.document_id = row.document_id,
      n.title = row.title,
      n.version = row.version,
      n.publisher = row.publisher,
      n.copyright = row.copyright,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Paragraph.csv  (24 rows, key paragraph_id)
node_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.paragraph_id})
  SET
      n:Paragraph,
      n.paragraph_id = row.paragraph_id,
      n.number = row.number,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_PointOfFocus.csv  (299 rows, key point_of_focus_id)
node_pointoffocus = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.point_of_focus_id})
  SET
      n:PointOfFocus,
      n.point_of_focus_id = row.point_of_focus_id,
      n.criterion_ref = row.criterion_ref,
      n.sequence = row.sequence,
      n.name = row.name,
      n.text = row.text,
      n.applicability = row.applicability,
      n.subgrouping = row.subgrouping,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (9 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.name = row.name,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_TrustServicesCategory.csv  (5 rows, key category_id)
node_trustservicescategory = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.category_id})
  SET
      n:TrustServicesCategory,
      n.category_id = row.category_id,
      n.letter = row.letter,
      n.name = row.name,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_APPLIES_TO_CATEGORY.csv -> :APPLIES_TO_CATEGORY  (193 rows)
rel_applies_to_category = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'APPLIES_TO_CATEGORY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:APPLIES_TO_CATEGORY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES_TERM.csv -> :DEFINES_TERM  (63 rows)
rel_defines_term = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES_TERM'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:DEFINES_TERM]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CATEGORY.csv -> :HAS_CATEGORY  (5 rows)
rel_has_category = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CATEGORY'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CATEGORY]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CRITERION.csv -> :HAS_CRITERION  (61 rows)
rel_has_criterion = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CRITERION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_CRITERION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_GROUP.csv -> :HAS_GROUP  (12 rows)
rel_has_group = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_GROUP'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_GROUP]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_PARAGRAPH.csv -> :HAS_PARAGRAPH  (24 rows)
rel_has_paragraph = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_PARAGRAPH'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_PARAGRAPH]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_POINT_OF_FOCUS.csv -> :HAS_POINT_OF_FOCUS  (299 rows)
rel_has_point_of_focus = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_POINT_OF_FOCUS'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_POINT_OF_FOCUS]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (9 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'SOC_2_TRUST_SERVICES_CRITERIA', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='SOC 2 Trust Services Criteria',
    folder=FOLDER,
    jurisdiction='United States',
    source_document='AICPA TSP Section 100 — 2017 Trust Services Criteria for Security, Availability, Processing Integrity, Confidentiality, and Privacy (includes March 2020 updates) [KB 2/USA/SOC 2.pdf]',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='CriteriaGroup', csv_name='nodes_CriteriaGroup.csv', id_column='group_id',
                 properties=['ref', 'name', 'source_document'],
                 rows=12, loadable=12, cypher=node_criteriagroup),
        NodeStep(label='Criterion', csv_name='nodes_Criterion.csv', id_column='criterion_id',
                 properties=['ref', 'series', 'coso_principle', 'text', 'source_document'],
                 rows=61, loadable=61, cypher=node_criterion),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['term', 'definition', 'source_document'],
                 rows=63, loadable=63, cypher=node_definition),
        NodeStep(label='Document', csv_name='nodes_Document.csv', id_column='document_id',
                 properties=['title', 'version', 'publisher', 'copyright', 'source_document'],
                 rows=1, loadable=1, cypher=node_document),
        NodeStep(label='Paragraph', csv_name='nodes_Paragraph.csv', id_column='paragraph_id',
                 properties=['number', 'heading', 'text', 'source_document'],
                 rows=24, loadable=24, cypher=node_paragraph),
        NodeStep(label='PointOfFocus', csv_name='nodes_PointOfFocus.csv', id_column='point_of_focus_id',
                 properties=['criterion_ref', 'sequence', 'name', 'text', 'applicability', 'subgrouping', 'source_document'],
                 rows=299, loadable=299, cypher=node_pointoffocus),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['name', 'source_document'],
                 rows=9, loadable=9, cypher=node_section),
        NodeStep(label='TrustServicesCategory', csv_name='nodes_TrustServicesCategory.csv', id_column='category_id',
                 properties=['letter', 'name', 'text', 'source_document'],
                 rows=5, loadable=5, cypher=node_trustservicescategory),
    ],
    rel_steps=[
        RelStep(rel_type='APPLIES_TO_CATEGORY', csv_name='rels_APPLIES_TO_CATEGORY.csv',
                properties=[],
                rows=193, loadable=193, cypher=rel_applies_to_category),
        RelStep(rel_type='DEFINES_TERM', csv_name='rels_DEFINES_TERM.csv',
                properties=[],
                rows=63, loadable=63, cypher=rel_defines_term),
        RelStep(rel_type='HAS_CATEGORY', csv_name='rels_HAS_CATEGORY.csv',
                properties=[],
                rows=5, loadable=5, cypher=rel_has_category),
        RelStep(rel_type='HAS_CRITERION', csv_name='rels_HAS_CRITERION.csv',
                properties=[],
                rows=61, loadable=61, cypher=rel_has_criterion),
        RelStep(rel_type='HAS_GROUP', csv_name='rels_HAS_GROUP.csv',
                properties=[],
                rows=12, loadable=12, cypher=rel_has_group),
        RelStep(rel_type='HAS_PARAGRAPH', csv_name='rels_HAS_PARAGRAPH.csv',
                properties=[],
                rows=24, loadable=24, cypher=rel_has_paragraph),
        RelStep(rel_type='HAS_POINT_OF_FOCUS', csv_name='rels_HAS_POINT_OF_FOCUS.csv',
                properties=[],
                rows=299, loadable=299, cypher=rel_has_point_of_focus),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=9, loadable=9, cypher=rel_has_section),
    ],
    expected_nodes_total=474,
    expected_rels_total=666,
)

if __name__ == "__main__":
    run(LOADER)
