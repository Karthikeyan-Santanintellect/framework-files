"""Loader — NAIC Insurance Data Security Model Law.

Source folder : New sources/NAIC Insurance Data Security Model Law
Source document(s):
    NAIC Insurance Data Security Model Law (Model #668), NAIC Model Laws, Regulations, Guidelines and Other Resources—4th Quarter 2017 (KB 2/USA/NAIC Insurance Data Security Model Law.pdf)

Graph shape   : 7 node labels, 7 relationship types
Expected size : 174 nodes / 196 relationships

Generated by App_new/tools/gen_new_source_loaders.py from the CSV
headers on disk. Edit the generator, not this file.

    python App_new/naic_insurance_data_security_model_law.py --dry-run   # validate, write nothing
    python App_new/naic_insurance_data_security_model_law.py             # load into Neo4j
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from new_sources_runtime import Loader, NodeStep, RelStep, run

FRAMEWORK_ID = 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW'
FOLDER = 'NAIC Insurance Data Security Model Law'

# --------------------------------------------------------------------------
# Framework root node. Labelled :NewSourceFramework, not :Framework —
# four folders ship a node of their own labelled Framework, and a bare
# :Framework merge key would silently fuse the root into it.
# --------------------------------------------------------------------------

constraint = """
CREATE CONSTRAINT new_source_node_key IF NOT EXISTS
FOR (n:NewSourceNode) REQUIRE (n.framework_id, n.node_id) IS UNIQUE;
"""

framework = """
MERGE (f:NewSourceFramework {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW'})
SET f.name = 'NAIC Insurance Data Security Model Law',
    f.jurisdiction = 'United States',
    f.folder = 'NAIC Insurance Data Security Model Law',
    f.source_document = 'NAIC Insurance Data Security Model Law (Model #668), NAIC Model Laws, Regulations, Guidelines and Other Resources—4th Quarter 2017 (KB 2/USA/NAIC Insurance Data Security Model Law.pdf)',
    f.status = 'extracted';
"""

# Attach the framework to every node that nothing else points at, so each
# of the 64 graphs has a single entry point and can be traversed or deleted
# independently of the other 63.
roots = """
MATCH (f:NewSourceFramework {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW'})
MATCH (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW'})
WHERE NOT (:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW'})-->(n)
MERGE (f)-[:HAS_ROOT]->(n);
"""

# --------------------------------------------------------------------------
# Nodes — one statement per nodes_<Label>.csv
# --------------------------------------------------------------------------

# nodes_Clause.csv  (99 rows, key clause_id)
node_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.clause_id})
  SET
      n:Clause,
      n.clause_id = row.clause_id,
      n.parent_id = row.parent_id,
      n.label = row.label,
      n.citation = row.citation,
      n.level = row.level,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Definition.csv  (16 rows, key definition_id)
node_definition = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.definition_id})
  SET
      n:Definition,
      n.definition_id = row.definition_id,
      n.section_id = row.section_id,
      n.letter = row.letter,
      n.term = row.term,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_DraftingNote.csv  (3 rows, key draftingnote_id)
node_draftingnote = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.draftingnote_id})
  SET
      n:DraftingNote,
      n.draftingnote_id = row.draftingnote_id,
      n.attached_to = row.attached_to,
      n.position = row.position,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Instrument.csv  (1 rows, key instrument_id)
node_instrument = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.instrument_id})
  SET
      n:Instrument,
      n.instrument_id = row.instrument_id,
      n.title = row.title,
      n.short_title = row.short_title,
      n.model_number = row.model_number,
      n.issuing_body = row.issuing_body,
      n.publication = row.publication,
      n.copyright = row.copyright,
      n.chronological_summary = row.chronological_summary,
      n.instrument_type = row.instrument_type,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Requirement.csv  (12 rows, key requirement_id)
node_requirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.requirement_id})
  SET
      n:Requirement,
      n.requirement_id = row.requirement_id,
      n.provision_id = row.provision_id,
      n.requirement_type = row.requirement_type,
      n.value = row.value,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Section.csv  (13 rows, key section_id)
node_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.section_id})
  SET
      n:Section,
      n.section_id = row.section_id,
      n.number = row.number,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# nodes_Subsection.csv  (30 rows, key subsection_id)
node_subsection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
CALL (row) {
  MERGE (n:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.subsection_id})
  SET
      n:Subsection,
      n.subsection_id = row.subsection_id,
      n.section_id = row.section_id,
      n.letter = row.letter,
      n.citation = row.citation,
      n.heading = row.heading,
      n.text = row.text,
      n.source_document = row.source_document
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
# Relationships — one statement per rel_type. The rel_type column is
# authoritative; a single rels_*.csv may carry several types.
# --------------------------------------------------------------------------

# rels_CROSS_REFERENCES.csv -> :CROSS_REFERENCES  (23 rows)
rel_cross_references = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'CROSS_REFERENCES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:CROSS_REFERENCES]->(t)
  SET r.quoted_reference = row.quoted_reference
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_DEFINES.csv -> :DEFINES  (16 rows)
rel_defines = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'DEFINES'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:DEFINES]->(t)
  SET r.term = row.term
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_CLAUSE.csv -> :HAS_CLAUSE  (99 rows)
rel_has_clause = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_CLAUSE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_CLAUSE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_DRAFTING_NOTE.csv -> :HAS_DRAFTING_NOTE  (3 rows)
rel_has_drafting_note = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_DRAFTING_NOTE'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_DRAFTING_NOTE]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SECTION.csv -> :HAS_SECTION  (13 rows)
rel_has_section = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_SECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_HAS_SUBSECTION.csv -> :HAS_SUBSECTION  (30 rows)
rel_has_subsection = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'HAS_SUBSECTION'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:HAS_SUBSECTION]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# rels_STATES_REQUIREMENT.csv -> :STATES_REQUIREMENT  (12 rows)
rel_states_requirement = """
LOAD CSV WITH HEADERS FROM '$file_path' AS row
WITH row WHERE row.rel_type = 'STATES_REQUIREMENT'
CALL (row) {
  MATCH (s:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.source_id})
  MATCH (t:NewSourceNode {framework_id: 'NAIC_INSURANCE_DATA_SECURITY_MODEL_LAW', node_id: row.target_id})
  MERGE (s)-[r:STATES_REQUIREMENT]->(t)
} IN TRANSACTIONS OF 500 ROWS;
"""

# --------------------------------------------------------------------------
LOADER = Loader(
    framework_id=FRAMEWORK_ID,
    name='NAIC Insurance Data Security Model Law',
    folder=FOLDER,
    jurisdiction='United States',
    source_document='NAIC Insurance Data Security Model Law (Model #668), NAIC Model Laws, Regulations, Guidelines and Other Resources—4th Quarter 2017 (KB 2/USA/NAIC Insurance Data Security Model Law.pdf)',
    constraint_cypher=constraint,
    framework_cypher=framework,
    root_cypher=roots,
    node_steps=[
        NodeStep(label='Clause', csv_name='nodes_Clause.csv', id_column='clause_id',
                 properties=['parent_id', 'label', 'citation', 'level', 'text', 'source_document'],
                 rows=99, loadable=99, cypher=node_clause),
        NodeStep(label='Definition', csv_name='nodes_Definition.csv', id_column='definition_id',
                 properties=['section_id', 'letter', 'term', 'text', 'source_document'],
                 rows=16, loadable=16, cypher=node_definition),
        NodeStep(label='DraftingNote', csv_name='nodes_DraftingNote.csv', id_column='draftingnote_id',
                 properties=['attached_to', 'position', 'text', 'source_document'],
                 rows=3, loadable=3, cypher=node_draftingnote),
        NodeStep(label='Instrument', csv_name='nodes_Instrument.csv', id_column='instrument_id',
                 properties=['title', 'short_title', 'model_number', 'issuing_body', 'publication', 'copyright', 'chronological_summary', 'instrument_type', 'source_document'],
                 rows=1, loadable=1, cypher=node_instrument),
        NodeStep(label='Requirement', csv_name='nodes_Requirement.csv', id_column='requirement_id',
                 properties=['provision_id', 'requirement_type', 'value', 'text', 'source_document'],
                 rows=12, loadable=12, cypher=node_requirement),
        NodeStep(label='Section', csv_name='nodes_Section.csv', id_column='section_id',
                 properties=['number', 'heading', 'text', 'source_document'],
                 rows=13, loadable=13, cypher=node_section),
        NodeStep(label='Subsection', csv_name='nodes_Subsection.csv', id_column='subsection_id',
                 properties=['section_id', 'letter', 'citation', 'heading', 'text', 'source_document'],
                 rows=30, loadable=30, cypher=node_subsection),
    ],
    rel_steps=[
        RelStep(rel_type='CROSS_REFERENCES', csv_name='rels_CROSS_REFERENCES.csv',
                properties=['quoted_reference'],
                rows=23, loadable=23, cypher=rel_cross_references),
        RelStep(rel_type='DEFINES', csv_name='rels_DEFINES.csv',
                properties=['term'],
                rows=16, loadable=16, cypher=rel_defines),
        RelStep(rel_type='HAS_CLAUSE', csv_name='rels_HAS_CLAUSE.csv',
                properties=[],
                rows=99, loadable=99, cypher=rel_has_clause),
        RelStep(rel_type='HAS_DRAFTING_NOTE', csv_name='rels_HAS_DRAFTING_NOTE.csv',
                properties=[],
                rows=3, loadable=3, cypher=rel_has_drafting_note),
        RelStep(rel_type='HAS_SECTION', csv_name='rels_HAS_SECTION.csv',
                properties=[],
                rows=13, loadable=13, cypher=rel_has_section),
        RelStep(rel_type='HAS_SUBSECTION', csv_name='rels_HAS_SUBSECTION.csv',
                properties=[],
                rows=30, loadable=30, cypher=rel_has_subsection),
        RelStep(rel_type='STATES_REQUIREMENT', csv_name='rels_STATES_REQUIREMENT.csv',
                properties=[],
                rows=12, loadable=12, cypher=rel_states_requirement),
    ],
    expected_nodes_total=174,
    expected_rels_total=196,
)

if __name__ == "__main__":
    run(LOADER)
